lmcache >= 0.3.9
# CuPy 14.1.0 imports pytest from cupy.testing._random. Use <14.1.0
# until a fixed newer release is verified for runtime images.
cupy-cuda13x < 14.1.0
nixl == 1.3.1rc2 # do-not-merge: test NIXL 1.3.1 RC2 integration (revert to a stable pin before merge)
mooncake-transfer-engine >= 0.3.8
