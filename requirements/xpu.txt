# Common dependencies
-r common.txt
# TODO(xpu-wheel-testing): triton-xpu doesn't publish a `triton`-named
# distribution on PyPI, but other transitive deps (e.g. xgrammar) require a
# package literally named `triton` to exist. wheels.vllm.ai/xpu will host a
# compat shim wheel (`triton==3.7.2+xpu` -> depends on `triton-xpu==3.7.2`)
# once this branch lands upstream. Until then we point at the internal test
# index below (which currently hosts the same shim wheel). --trusted-host is
# only needed because the internal test index is plain HTTP; it will be
# removed once wheels.vllm.ai/xpu (HTTPS) replaces it.
--extra-index-url=http://10.239.182.107:8081/simple/
--trusted-host=10.239.182.107
triton==3.7.2+xpu

ray>=2.9
cmake>=3.26.1
packaging>=24.2
setuptools-scm>=8
setuptools-rust>=1.9.0
setuptools>=77.0.3,<81.0.0
wheel
jinja2>=3.1.6
datasets # for benchmark scripts
numba == 0.65.0 # Required for N-gram speculative decoding
--extra-index-url=https://download.pytorch.org/whl/xpu
torch==2.13.0+xpu
torchaudio==2.11.0+xpu
torchvision==0.28.0+xpu
torchcodec >= 0.14 # Required for the torchcodec video decoding backend

auto_round_lib==0.14.2
vllm_xpu_kernels==0.1.12.1
