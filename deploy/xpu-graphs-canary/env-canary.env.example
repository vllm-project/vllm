# Canary profile — copy into a separate DS/Job, not Ornith production.

VLLM_XPU_ENABLE_XPU_GRAPH=1
ZE_AFFINITY_MASK=0

# Serve args (example — splice into container args; omit --enforce-eager):
#   --attention-backend TRITON_ATTN
#   -cc.cudagraph_mode=PIECEWISE
