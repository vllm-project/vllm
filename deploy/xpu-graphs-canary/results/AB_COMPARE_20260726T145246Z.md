# XPU graphs canary A/B — 20260726T145246Z

**Model:** `Qwen/Qwen2.5-Coder-7B-Instruct`  
**Build:** `docker.io/hal/vllm-xpu:kris-fork-577e1a932` + hostPath overlay of `xpu-graphs-triton-piecewise-canary` (`VLLM_XPU_GRAPH_FORCE_PIECEWISE` present)

| Phase | Config | median output tok/s | runs | smoke |
|-------|--------|---------------------|------|-------|
| **before** | graphs=0 + `--enforce-eager` + TRITON_ATTN | **142.55** | [142.83, 142.27, 142.55] | PASS |
| **after** | graphs=1 + TRITON_ATTN + `-cc.cudagraph_mode=PIECEWISE` | **143.4** | [143.4, 143.46, 143.36] | PASS |

**Delta (after vs before):** 0.60%

### Correctness
- Before short/long: PASS (2+2 → 4; coherent long form; temp=0 match)
- After short/long: PASS
- Failed requests: 0 on all timed bench runs

### Serve log signals
**Before:** enforce-eager / cudagraph NONE / Triton backend  
**After:** XPU Graph experimental warning; `cudagraph_mode: PIECEWISE`; Triton backend; no `work_group_scratch_memory` crash

### Interpretation
Aggregate `vllm bench serve` output tok/s is essentially **flat** (~143 tok/s) on this dense 7B + TRITON + PIECEWISE synthetic load (16 concurrent, 256/128). The canary **boots and decodes correctly** with graphs on — the prior FA+FULL SYCL Graph crash is avoided. Do not expect large tok/s wins from PIECEWISE alone on this workload; Ornith remains on eager until a clearer win or longer soak.

Ornith DS was paused for the window and restored afterward.
