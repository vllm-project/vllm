# FA-in-graph A/B — 20260727T151703Z

**Model:** `Qwen/Qwen2.5-0.5B-Instruct` (local snapshot, bf16)
**Image:** `hal/vllm-xpu:oneapi-2026.0-torch2.13-3deb3160c` + branch overlay (`fa-in-graph-dense-validation`)
**Device:** Intel Arc Pro B70 (single GPU); greedy; 8 streamed completions of 128 tokens after warmup.
**Runtime:** 2.13.0+xpu 20260000 2026.0.0 3.7.2 0.1.12.dev33+gaa15657 (torch / torch.version.xpu / intel-sycl-rt / triton-xpu / vllm-xpu-kernels)

| Arm | TTFT ms mean | TTFT ms p50 | Decode tok/s mean | Decode tok/s p50 |
| --- | --- | --- | --- | --- |
| A: eager (`--enforce-eager`, graphs off) | 36.1 | 36.0 | 67.0 | 67.0 |
| B: FA-in-graph (FLASH_ATTN + `cudagraph_mode=FULL`, `FORCE_PIECEWISE=0`) | 22.6 | 22.6 | 401.6 | 401.8 |

**Decode tok/s delta (B vs A):** +499.5%
**TTFT delta (B vs A):** -37.6%

### Correctness

- **Per-arm temp=0 determinism:** same prompt repeated is byte-identical within each arm (A stable=True, B stable=True).
- **FA-in-graph vs eager greedy:** identical for the first **681 chars** (~all but the last ~1-2 tokens of a 128-token generation); they diverge only at the tail (`...data processing tasks` vs `...data transformations`), a bf16 greedy near-tie flip between eager and graph-captured kernels. Both continuations are coherent.
- No NaN, no `!{4,}` loops, no empty/garbage output in either arm.
- Short (`2+2` -> `4`): PASS both arms.

### Serve-log signals (arm B, branch code)
- `FlashAttention-in-graph enabled: capturing full XPU Graph (mode FULL) including FlashAttention on oneAPI 2026.0+ runtime.`
- Captured 51 PIECEWISE + 35 FULL graphs; `cudagraph_mode=FULL` -> `FULL_AND_PIECEWISE` (FA2 `UNIFORM_BATCH` downgrade).
- No `work_group_scratch_memory` / SYCL Graph error.
- `VLLM_XPU_GRAPH_FORCE_PIECEWISE` registered (no unknown-env-variable warning).

### Interpretation
On the oneAPI 2026.0 base, capturing FlashAttention into a full XPU Graph is correct and ~5x faster single-stream decode than eager on this tiny dense model (same magnitude 02 saw for FULL graphs). Greedy output tracks eager to the last token or two, diverging only via expected bf16 numerics — no corruption. The 01 scratch-in-graph blocker is gone. Production Ornith stays eager; this validates dense FA-in-graph only.
