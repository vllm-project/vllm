# In this file, you can add more tests to run either by adding a new step or
# adding a new command to an existing step. See different options here for examples.

# This script will be feed into Jinja template in `test-template-amd.j2` at
# https://github.com/vllm-project/buildkite-ci/blob/main/scripts/test-template-amd.j2
# to generate the final pipeline yaml file.

# Documentation
# label(str): the name of the test. emojis allowed.
# fast_check(bool): whether to run this on each commit on the fastcheck pipeline.
# torch_nightly(bool): whether to run this on vllm against the torch nightly pipeline.
# fast_check_only(bool): run this test on the fastcheck pipeline only
# optional(bool): never run this test by default (i.e. need to unblock manually) unless it's a scheduled nightly run.
# soft_fail(bool): allow this step to fail without failing the entire pipeline (useful for flaky or experimental tests).
# command(str): the single command to run for tests. incompatible with commands.
# commands(list): the list of commands to run for the test. incompatible with command.
# mirror_hardwares(list): the list of hardware to run the test on as well. currently only supports [amdexperimental]
# num_gpus(int): override the number of GPUs for the test. defaults to 1 GPU. currently supports 2,4.
# num_nodes(int): whether to simulate multi-node setup by launching multiple containers on one host,
#     in this case, commands must be specified. the first command runs on the first host, the second
#     command runs on the second host.
# timeout_in_minutes(int): sets a timeout for the step in minutes. if not specified, uses the default timeout.
# parallelism(int): number of parallel jobs to run for this step. enables test sharding using $$BUILDKITE_PARALLEL_JOB
#     and $$BUILDKITE_PARALLEL_JOB_COUNT environment variables.
# working_dir(str): specify the place where the command should execute, default to /vllm-workspace/tests
# source_file_dependencies(list): the list of prefixes to opt-in the test for, if empty, the test will always run.

# When adding a test
# - If the test belongs to an existing group, add it there
# - If the test is short, add to any existing step
# - If the test takes more than 10min, then it is okay to create a new step.
#   Note that all steps execute in parallel.


#####################################################################################################################################
#                                                                                                                                   #
#                                                             README                                                                #
#                                                                                                                                   #
#####################################################################################################################################
#                                                                                                                                   #
# IMPORTANT:                                                                                                                        #
#   * This reduced AMD CI definition targets MI300 agents only. All upcoming feature improvements are tracked in:                  #
#     https://github.com/vllm-project/vllm/issues/34994                                                                            #
#                                                                                                                                   #
#-----------------------------------------------------------------------------------------------------------------------------------#
#                                                                                                                                   #
# NOTES:                                                                                                                            #
#   * [Pytorch Nightly Dependency Override Check]: if this test fails, it means the nightly torch version is not compatible with    #
#                                                  some of the dependencies. Please check the error message and add the package to  #
#                                                  whitelist in `/vllm/tools/pre_commit/generate_nightly_torch_test.py`.            #
#   * [Entrypoints Integration (LLM)]:                                                                                              #
#     - {`pytest -v -s entrypoints/llm/test_generate.py`}: It needs a clean process                                                 #
#     - {`pytest -v -s entrypoints/offline_mode`}: Needs to avoid interference with other tests                                     #
#   * [Engine / Engine (1 GPU) / e2e Scheduling / e2e Core / V1 e2e / Spec Decode / V1 Sample + Logits / V1 Core + KV + Metrics]:   #
#     - Previously a single "V1 Test e2e + engine" step, now split across multiple groups.                                          #
#     - V1 e2e (2/4 GPUs) uses 4 GPUs but is scheduled on 8-GPU machines for stability. See:                                        #
#       https://github.com/vllm-project/vllm/pull/31040                                                                             #
#   * [V1 Sample + Logits / V1 Core + KV + Metrics / V1 others (CPU)]:                                                              #
#     - Previously a single "V1 others" step, now split to avoid interference.                                                      #
#     - Integration test for streaming correctness (requires special branch for __harness__ lib).                                   #
#   * [V1 others (CPU)]: Split the tests to avoid interference                                                                      #
#   * [PyTorch Compilation Unit Tests]: Run unit tests defined directly under `compile/`, not including subdirectories, which       #
#                                       are usually heavier tests covered elsewhere. Use `find` to launch multiple instances        #
#                                       of pytest so that they do not suffer from:                                                  #
#                                       https://github.com/vllm-project/vllm/issues/28965                                           #
#   * [PyTorch Fullgraph Smoke Test]: Run smoke tests under fullgraph directory, except `test_full_graph.py` as it is a heavy       #
#                                     test that is covered in other steps. Use `find` to launch multiple instances of pytest        #
#                                     so that they do not suffer from: https://github.com/vllm-project/vllm/issues/28965            #
#   * [PyTorch Fullgraph]:                                                                                                          #
#     - Limit to no custom ops to reduce running time. Wrap with quotes to escape yaml and avoid starting `-k` string               #
#       with a `-`                                                                                                                  #
#     - Old E2E tests such as:                                                                                                      #
#           ```bash                                                                                                                 #
#           pytest -v -s compile/distributed/test_fusions_e2e.py -k 'TRITON and not +quant_fp8 and not Llama-4'                     #
#           ```                                                                                                                     #
#       were removed in https://github.com/vllm-project/vllm/pull/33293 in favor of new tests in `fusions_e2e`. We                  #
#       avoid replicating the new jobs in this file as it's deprecated.                                                             #
#   * [Basic Models Tests (Extra Initialization) %N]: Only when vLLM model source is modified - test initialization of a            #
#                                                     large subset of supported models (the complement of the small subset in       #
#                                                     the above test.) Also run if model initialization test file is modified.      #
#   * [Language Models Tests (Extra Standard) %N]: Shard slow subset of standard language models tests. Only run when model         #
#                                                  source is modified, or when specified test files are modified.                   #
#   * [Language Models Tests (Hybrid) %N]: Install fast path packages for testing against transformers (mamba, conv1d) and to       #
#                                          run plamo2 model in vLLM.                                                                #
#   * [Language Models Test (Extended Generation)]: Install fast path packages for testing against transformers (mamba, conv1d)     #
#                                                   and to run plamo2 model in vLLM.                                                #
#   * [Multi-Modal Models (Standard) 1-4]:                                                                                          #
#     - Do NOT remove `VLLM_WORKER_MULTIPROC_METHOD=spawn` setting as ROCm requires this for certain models to function.            #
#   * [Transformers Nightly Models]: Whisper needs `VLLM_WORKER_MULTIPROC_METHOD=spawn` to avoid deadlock.                          #
#   * [Plugin Tests (2 GPUs)]:                                                                                                      #
#     - {`pytest -v -s plugins_tests/test_oot_registration_online.py`}: It needs a clean process                                    #
#     - {`pytest -v -s plugins_tests/test_oot_registration_offline.py`}: It needs a clean process                                   #
#     - {`pytest -v -s plugins_tests/lora_resolvers`}: Unit tests for in-tree lora resolver plugins                                 #
#   * [LoRA TP (Distributed)]:                                                                                                      #
#     - There is some Tensor Parallelism related processing logic in LoRA that requires multi-GPU testing for validation.           #
#     - {`pytest -v -s -x lora/test_gptoss_tp.py`}: Disabled for now because MXFP4 backend on non-cuda platform doesn't support     #
#                                                   LoRA yet.                                                                       #
#   * [Distributed Tests (NxGPUs)(HW-TAG)]: Don't test llama model here, it seems hf implementation is buggy. See:                  #
#                                           https://github.com/vllm-project/vllm/pull/5689                                          #
#   * [Distributed Tests (NxGPUs)(HW-TAG)]: Some old E2E tests were removed in https://github.com/vllm-project/vllm/pull/33293      #
#                                           in favor of new tests in fusions_e2e. We avoid replicating the new jobs in              #
#                                           this file as it's deprecated.                                                           #
#                                                                                                                                   #
#####################################################################################################################################


steps:

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

- label: PyTorch Compilation Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - "find compile/ -maxdepth 1 -name 'test_*.py' -print0 | xargs -0 -n1 -I{} pytest -s -v '{}'"

- label: PyTorch Fullgraph Smoke Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - "find compile/fullgraph/ -name 'test_*.py' -not -name 'test_full_graph.py' -exec pytest -s -v {} \\\\;"

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Pipeline + Context Parallelism (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_pp_cudagraph.py
  - pytest -v -s distributed/test_pipeline_parallel.py

#----------------------------------------------------------  mi300 · kernels  ----------------------------------------------------------#

- label: Kernels Helion Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install helion==1.1.0
  - pytest -v -s kernels/helion/

- label: Kernels Mamba Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/mamba

#------------------------------------------------------  mi300 · models / basic  -------------------------------------------------------#

- label: Basic Models Test (Other CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_utils.py models/test_vision.py

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: Language Models Test (MTEB) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/pooling_mteb_test

- label: Batch Invariance (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pip install pytest-timeout pytest-forked
  - pytest -v -s v1/determinism/test_batch_invariance.py
  - pytest -v -s v1/determinism/test_rms_norm_batch_invariant.py

- label: Cudagraph # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/cudagraph/test_cudagraph_dispatch.py
  - pytest -v -s v1/cudagraph/test_cudagraph_mode.py

- label: e2e Core (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/general --ignore v1/e2e/general/test_async_scheduling.py

- label: Async Engine, Inputs, Utils, Worker, Config (CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - python3 standalone_tests/lazy_imports.py
  - pytest -v -s test_inputs.py
  - pytest -v -s test_outputs.py
  - pytest -v -s test_pooling_params.py
  - pytest -v -s test_ray_env.py
  - pytest -v -s -m 'cpu_test' multimodal
  - pytest -v -s renderers
  - pytest -v -s tokenizers_
  - pytest -v -s reasoning
  - pytest -v -s tool_parsers
  - pytest -v -s parser
  - pytest -v -s transformers_utils
  - pytest -v -s config

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#-----------------------------------------------------  mi300 · basic_correctness  -----------------------------------------------------#

- label: Basic Correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s basic_correctness/test_mem.py
  - VLLM_TARGET_TEST_SUITE=MI300 pytest -v -s basic_correctness/test_basic_correctness.py
  - pytest -v -s basic_correctness/test_cpu_offload.py

- label: Distributed Model Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - TARGET_TEST_SUITE=MI300 pytest basic_correctness/ -v -s -m 'distributed(num_gpus=2)'
  - CUDA_VISIBLE_DEVICES=0,1 pytest -v -s model_executor/model_loader/test_sharded_state_loader.py -m '(not slow_test)'
  - pytest models/test_transformers.py -v -s -m 'distributed(num_gpus=2)'
  - pytest models/language -v -s -m 'distributed(num_gpus=2)'
  - pytest models/multimodal -v -s -m 'distributed(num_gpus=2)' --ignore models/multimodal/generation/test_whisper.py --ignore models/multimodal/generation/test_phi4siglip.py
  - pytest models/multimodal/generation/test_phi4siglip.py -v -s -m 'distributed(num_gpus=2)'
  - VLLM_WORKER_MULTIPROC_METHOD=spawn pytest models/multimodal/generation/test_whisper.py -v -s -m 'distributed(num_gpus=2)'

#--------------------------------------------------------  mi300 · benchmarks  ---------------------------------------------------------#

- label: Benchmarks CLI Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s benchmarks/

#----------------------------------------------------------  mi300 · compile  ----------------------------------------------------------#

- label: Fusion E2E Config Sweep (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace/"
  commands:
  - rocm-smi
  - pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k "llama-3"

- label: Fusion E2E Quick (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace/"
  commands:
  - rocm-smi
  # Run all models and attn backends but only Inductor partition and native custom ops
  - "pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k 'inductor_partition and not +rms_norm and not +quant_fp8'"
  # Different from CUDA, Qwen requires +rms_norm and +quant_fp8 as rms+quant fusion is only supported on AITER
  - "pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k 'inductor_partition and +rms_norm and +quant_fp8 and qwen3'"

- label: PyTorch Compilation Passes Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -s -v compile/passes --ignore compile/passes/distributed

- label: PyTorch Fullgraph # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s compile/fullgraph/test_full_graph.py -k 'not test_fp8_kv_scale_compile'

- label: Pytorch Nightly Dependency Override Check # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  soft_fail: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash standalone_tests/pytorch_nightly_dependency.sh

- label: Distributed Compile Unit Tests (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - export VLLM_TEST_CLEAN_GPU_MEMORY=1
  - VLLM_TEST_CLEAN_GPU_MEMORY=1 pytest -v -s tests/compile/passes/distributed/test_async_tp.py
  - pytest -v -s tests/compile/fusions_e2e/test_tp2_ar_rms.py::test_tp2_ar_rms_fusions

#-----------------------------------------------------------  mi300 · cuda  ------------------------------------------------------------#

- label: Platform Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s cuda/test_cuda_context.py
  - pytest -v -s cuda/test_platform_no_cuda_init.py

#--------------------------------------------------------  mi300 · detokenizer  --------------------------------------------------------#

- label: Async Engine, Inputs, Utils, Worker # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s detokenizer
  - pytest -v -s -m 'not cpu_test' multimodal
  - pytest -v -s utils_

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Distributed Comm Ops # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_comm_ops.py
  - pytest -v -s distributed/test_shm_broadcast.py
  - pytest -v -s distributed/test_shm_buffer.py
  - pytest -v -s distributed/test_shm_storage.py

- label: EPLB Algorithm # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_eplb_algo.py
  - pytest -v -s distributed/test_eplb_utils.py

- label: EPLB Execution # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_eplb_execute.py
  - pytest -v -s distributed/test_eplb_spec_decode.py

- label: Distributed Tests (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - pytest -v -s tests/distributed/test_context_parallel.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 examples/rl/rlhf_async_new_apis.py
  - VLLM_LOGGING_LEVEL=DEBUG python3 examples/features/data_parallel/data_parallel_offline.py --model=Qwen/Qwen1.5-MoE-A2.7B -tp=1 -dp=2 --max-model-len=2048 --all2all-backend=deepep_high_throughput
  - VLLM_LOGGING_LEVEL=DEBUG python3 examples/features/data_parallel/data_parallel_offline.py --model=Qwen/Qwen1.5-MoE-A2.7B -tp=1 -dp=2 --max-model-len=2048 --all2all-backend=allgather_reducescatter --disable-nccl-for-dp-synchronization
  - pytest -v -s tests/v1/distributed/test_dbo.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 pytest -v -s tests/distributed/test_weight_transfer.py
  - pytest -v -s tests/distributed/test_packed_tensor.py

- label: Distributed Tests (4xA100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_custom_all_reduce.py
  - torchrun --nproc_per_node=2 distributed/test_ca_buffer_sharing.py
  - TARGET_TEST_SUITE=MI300 pytest basic_correctness/ -v -s -m 'distributed(num_gpus=2)'
  - pytest -v -s -x lora/test_mixtral.py

- label: Distributed Torchrun + Examples (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - torchrun --nproc-per-node=4 distributed/test_torchrun_example.py
  - PP_SIZE=2 torchrun --nproc-per-node=4 distributed/test_torchrun_example.py
  - TP_SIZE=4 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - PP_SIZE=2 TP_SIZE=2 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - DP_SIZE=4 ENABLE_EP=1 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - TP_SIZE=2 DP_SIZE=2 ENABLE_EP=1 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - python3 ../examples/features/data_parallel/data_parallel_offline.py --enforce-eager
  # rlhf examples
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 ../examples/rl/rlhf_nccl.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 ../examples/rl/rlhf_ipc.py

- label: Elastic EP Scaling Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_elastic_ep.py

- label: RayExecutorV2 (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_USE_RAY_V2_EXECUTOR_BACKEND=1
  - pytest -v -s distributed/test_ray_v2_executor.py
  - pytest -v -s distributed/test_ray_v2_executor_e2e.py
  - pytest -v -s distributed/test_pipeline_parallel.py -k "ray"
  - TARGET_TEST_SUITE=L4 pytest -v -s basic_correctness/test_basic_correctness.py -k "ray"

- label: Distributed Tests (8xH100-8xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/tests"
  commands:
  - torchrun --nproc-per-node=8 ../examples/features/torchrun/torchrun_dp_example_offline.py --tp-size=2 --pp-size=1 --dp-size=4 --enable-ep

#--------------------------------------------------------  mi300 · entrypoints  --------------------------------------------------------#

- label: Entrypoints Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s entrypoints/unit_tests
  - pytest -v -s entrypoints/weight_transfer

- label: Entrypoints Integration (LLM) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/llm --ignore=entrypoints/llm/test_generate.py --ignore=entrypoints/llm/test_collective_rpc.py --ignore=entrypoints/llm/offline_mode
  - pytest -v -s entrypoints/llm/test_generate.py # it needs a clean process
  - pytest -v -s entrypoints/llm/offline_mode # Needs to avoid interference with other tests

- label: Entrypoints Integration (API Server) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/serve --ignore=entrypoints/serve/dev/rpc
  - PYTHONPATH=/vllm-workspace pytest -v -s entrypoints/serve/dev/rpc

- label: Entrypoints Integration (API Server OpenAI - Part 1) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai --ignore=entrypoints/openai/completion --ignore=entrypoints/openai/chat_completion --ignore=entrypoints/openai/responses --ignore=entrypoints/openai/correctness

- label: Entrypoints Integration (API Server OpenAI - Part 2) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai/chat_completion
  - pytest -v -s entrypoints/openai/completion --ignore=entrypoints/openai/completion/test_tensorizer_entrypoint.py

- label: Entrypoints Integration (API Server Generate) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s tool_use
  - pytest -v -s entrypoints/tool_parsers
  - pytest -v -s entrypoints/generate
  - pytest -v -s entrypoints/anthropic

- label: Entrypoints Integration (Responses API) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai/responses

- label: Entrypoints Integration (Speech to Text) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/speech_to_text

- label: Entrypoints Integration (Multimodal)
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/multimodal

- label: Entrypoints Integration (Pooling) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/pooling

- label: OpenAI API correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash ../tools/install_torchcodec_rocm.sh || exit 1
  - pytest -s entrypoints/openai/correctness/

#-----------------------------------------------------------  mi300 · evals  -----------------------------------------------------------#

- label: DeepSeek V2-Lite Prefetch Offload Accuracy (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/deepseek_v2_lite_prefetch_offload.sh 0.25 200 8030

- label: LM Eval Small Models # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -s -v evals/gsm8k/test_gsm8k_correctness.py --config-list-file=configs/models-small.txt

- label: LM Eval Small Models (MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-small-rocm.txt

- label: Multi-Modal Accuracy Eval (Small Models) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-mm-small.txt --tp-size=1

- label: GPQA Eval (GPT-OSS) (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
    - uv pip install --system 'gpt-oss[eval]==0.0.5'
    - pytest -s -v evals/gpt_oss/test_gpqa_correctness.py --config-list-file=configs/models-gfx942.txt

- label: DeepSeek V2-Lite Sync EPLB Accuracy (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/deepseek_v2_lite_ep_eplb.sh 0.25 200 8010

- label: LM Eval Large Models (4xA100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large.txt --tp-size=4

- label: Qwen3-30B-A3B-FP8-block Sync EPLB Accuracy (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/qwen30b_a3b_fp8_block_ep_eplb.sh 0.8 200 8020

- label: Qwen3-Next-80B-A3B-Instruct MTP Async EPLB Accuracy # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/qwen3_next_mtp_async_eplb.sh 0.8 1319 8040

- label: LM Eval Large Models (8xH200-8xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v evals/gsm8k/test_gsm8k_correctness.py --config-list-file=configs/models-mi3xx.txt

#---------------------------------------------------------  mi300 · examples  ----------------------------------------------------------#

- label: Examples # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/examples"
  commands:
    - pip install tensorizer
    # Basic
    - python3 basic/offline_inference/chat.py
    - python3 basic/offline_inference/generate.py --model facebook/opt-125m
    - python3 basic/offline_inference/generate.py --model meta-llama/Llama-2-13b-chat-hf --cpu-offload-gb 10
    - python3 basic/offline_inference/classify.py
    - python3 basic/offline_inference/embed.py
    - python3 basic/offline_inference/score.py
    # Multi-modal models
    - python3 generate/multimodal/audio_language_offline.py --seed 0
    - python3 generate/multimodal/vision_language_offline.py --seed 0
    - python3 generate/multimodal/vision_language_multi_image_offline.py --seed 0
    - python3 generate/multimodal/encoder_decoder_multimodal_offline.py --model-type whisper --seed 0
    # Pooling models
    - python3 pooling/embed/vision_embedding_offline.py --seed 0
    # Features demo
    - python3 features/automatic_prefix_caching/prefix_caching_offline.py
    - python3 deployment/llm_engine_example.py
    - python3 features/tensorize_vllm_model.py --model facebook/opt-125m serialize --serialized-directory /tmp/ --suffix v1 && python3 features/tensorize_vllm_model.py --model facebook/opt-125m deserialize --path-to-tensors /tmp/vllm/facebook/opt-125m/v1/model.tensors
    - python3 features/speculative_decoding/spec_decode_offline.py --test --method eagle --num_spec_tokens 3 --dataset-name hf --dataset-path philschmid/mt-bench --num-prompts 80 --temp 0 --top-p 1.0 --top-k -1 --tp 1 --enable-chunked-prefill --max-model-len 2048
    - python3 features/speculative_decoding/spec_decode_offline.py --test --method eagle3 --num_spec_tokens 3 --dataset-name hf --dataset-path philschmid/mt-bench --num-prompts 80 --temp 0 --top-p 1.0 --top-k -1 --tp 1 --enable-chunked-prefill --max-model-len 1536

#----------------------------------------------------------  mi300 · kernels  ----------------------------------------------------------#

- label: Kernels Attention Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/attention --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: Kernels Core Operation Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/core --ignore=kernels/core/test_minimax_reduce_rms.py  kernels/test_concat_mla_q.py kernels/test_top_k_per_row.py

- label: Kernels MoE Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 5
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/moe --ignore=kernels/moe/test_modular_oai_triton_moe.py --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT
  - pytest -v -s kernels/moe/test_modular_oai_triton_moe.py --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: Kernels Quantization Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/quantization --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: Kernels FP8 MoE Test (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s kernels/moe/test_deepep_moe.py

#-----------------------------------------------------------  mi300 · lora  ------------------------------------------------------------#

- label: LoRA %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s lora --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --ignore=lora/test_chatglm3_tp.py --ignore=lora/test_llama_tp.py --ignore=lora/test_qwen3_with_multi_loras.py --ignore=lora/test_olmoe_tp.py --ignore=lora/test_deepseekv2_tp.py --ignore=lora/test_gptoss_tp.py --ignore=lora/test_qwen3moe_tp.py --ignore=lora/test_qwen35_densemodel_lora.py

- label: LoRA TP (Distributed) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
  - pytest -v -s -x lora/test_chatglm3_tp.py
  - pytest -v -s -x lora/test_llama_tp.py
  - pytest -v -s -x lora/test_qwen3_with_multi_loras.py
  - pytest -v -s -x lora/test_olmoe_tp.py
  - pytest -v -s -x lora/test_gptoss_tp.py
  - pytest -v -s -x lora/test_qwen35_densemodel_lora.py

#------------------------------------------------------  mi300 · model_executor  -------------------------------------------------------#

- label: Model Executor # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - apt-get update && apt-get install -y curl libsodium23
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s model_executor -m '(not slow_test)'
  - pytest -v -s entrypoints/openai/completion/test_tensorizer_entrypoint.py

#------------------------------------------------------  mi300 · models / basic  -------------------------------------------------------#

- label: Basic Models Tests (Extra Initialization) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_initialization.py -k 'not test_can_initialize_small_subset' --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

- label: Basic Models Tests (Initialization) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_initialization.py::test_can_initialize_small_subset

- label: Basic Models Tests (Other) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_terratorch.py models/test_transformers.py models/test_registry.py

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: Language Models Test (Extended Pooling)  # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/pooling -m 'not core_model'

- label: Language Models Tests (Standard) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip freeze | grep -E 'torch'
  - pytest -v -s models/language -m 'core_model and (not slow_test)'

- label: Language Models Tests (Extra Standard) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip freeze | grep -E 'torch'
  - pytest -v -s models/language -m 'core_model and slow_test' --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#----------------------------------------------------  mi300 · models / multimodal  ----------------------------------------------------#

- label: Multi-Modal Models (Extended Generation 1) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation -m 'not core_model' --ignore models/multimodal/generation/test_common.py
  - pytest -v -s models/multimodal/test_mapping.py

- label: Multi-Modal Models (Extended Generation 2) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m 'split(group=0) and not core_model'


- label: Multi-Modal Models (Extended Generation 3) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m 'split(group=1) and not core_model'

- label: "Multi-Modal Models (Standard) 1: qwen2" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "qwen2"
  - pytest -v -s models/multimodal/generation/test_ultravox.py -m core_model

- label: "Multi-Modal Models (Standard) 3: llava + qwen2_vl" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "not qwen2 and not qwen3 and not gemma"
  - pytest -v -s models/multimodal/generation/test_qwen2_vl.py -m core_model

- label: "Multi-Modal Models (Standard) 4: other + whisper" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal -m core_model --ignore models/multimodal/generation/test_common.py --ignore models/multimodal/generation/test_ultravox.py --ignore models/multimodal/generation/test_qwen2_5_vl.py --ignore models/multimodal/generation/test_qwen2_vl.py --ignore models/multimodal/generation/test_whisper.py  --ignore models/multimodal/generation/test_memory_leak.py --ignore models/multimodal/processing
  - pytest -v -s models/multimodal/generation/test_memory_leak.py -m core_model
  - cd .. && VLLM_WORKER_MULTIPROC_METHOD=spawn pytest -v -s tests/models/multimodal/generation/test_whisper.py -m core_model

- label: Multi-Modal Processor # 1h 42m
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/processing/test_tensor_schema.py

- label: Multi-Modal Processor (CPU) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/processing --ignore models/multimodal/processing/test_tensor_schema.py --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#-----------------------------------------------------  mi300 · models / quantized  -----------------------------------------------------#

- label: Quantized Models Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/quantization

#--------------------------------------------------  mi300 · models / transformers  ---------------------------------------------------#

- label: Transformers Nightly Models (Shardable) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  working_dir: "/vllm-workspace/"
  commands:
  - pip install --upgrade git+https://github.com/huggingface/transformers
  - pytest -v -s tests/models/test_initialization.py --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB
  - pytest -v -s tests/models/multimodal/processing/ --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

- label: Transformers Nightly Models (Single) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/"
  commands:
  - pip install --upgrade git+https://github.com/huggingface/transformers
  - pytest -v -s tests/models/test_transformers.py
  - pytest -v -s tests/models/multimodal/test_mapping.py
  - python3 examples/basic/offline_inference/chat.py
  - python3 examples/generate/multimodal/vision_language_offline.py --model-type qwen2_5_vl
  - VLLM_WORKER_MULTIPROC_METHOD=spawn python3 examples/generate/multimodal/audio_language_offline.py --model-type whisper

#----------------------------------------------------------  mi300 · plugins  ----------------------------------------------------------#

- label: Plugin Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  # BEGIN: platform plugin and general plugin tests, all the code in-between runs on dummy platform
  - pip install -e ./plugins/vllm_add_dummy_platform
  - pytest -v -s plugins_tests/test_platform_plugins.py
  - pip uninstall vllm_add_dummy_platform -y
  # END: platform plugin tests
  # BEGIN: `io_processor` plugins test, all the code in between uses the `prithvi_io_processor` plugin
  - pip install -e ./plugins/prithvi_io_processor_plugin
  - pytest -v -s plugins_tests/test_io_processor_plugins.py
  - pytest -v -s plugins_tests/test_terratorch_io_processor_plugins.py
  - pip uninstall prithvi_io_processor_plugin -y
  # END: `io_processor` plugins test
  # BEGIN: `bge_m3_sparse io_processor` test
  - pip install -e ./plugins/bge_m3_sparse_plugin
  - pytest -v -s plugins_tests/test_bge_m3_sparse_io_processor_plugins.py
  - pip uninstall bge_m3_sparse_plugin -y
  # END: `bge_m3_sparse io_processor` test
  # BEGIN: `colbert_query io_processor` test
  - pip install -e ./plugins/colbert_query_plugin
  - pytest -v -s plugins_tests/test_colbert_query_io_processor_plugins.py
  - pip uninstall colbert_query_plugin -y
  # END: `colbert_query io_processor` test
  # BEGIN: `stat_logger` plugins test
  - pip install -e ./plugins/vllm_add_dummy_stat_logger
  - pytest -v -s plugins_tests/test_stats_logger_plugins.py
  - pip uninstall dummy_stat_logger -y
  # END: `stat_logger` plugins test
  # BEGIN: other tests
  - pytest -v -s plugins_tests/test_scheduler_plugins.py
  - pip install -e ./plugins/vllm_add_dummy_model
  - pytest -v -s distributed/test_distributed_oot.py
  - pytest -v -s plugins_tests/test_oot_registration_online.py # it needs a clean process
  - pytest -v -s plugins_tests/test_oot_registration_offline.py # it needs a clean process
  - pytest -v -s plugins_tests/lora_resolvers # unit tests for in-tree lora resolver plugins

#-------------------------------------------------------  mi300 · quantization  --------------------------------------------------------#

- label: Quantization # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:

  # temporary install here since we need nightly, will move to requirements/test.in
  # after torchao 0.12 release, and pin a working version of torchao nightly here

  # since torchao nightly is only compatible with torch nightly currently
  # https://github.com/pytorch/ao/issues/2919, we'll have to skip new torchao tests for now
  # we can only upgrade after this is resolved
  # TODO(jerryzh168): resolve the above comment
  - uv pip install --system torchao==0.17.0
  - uv pip install --system conch-triton-kernels
  - VLLM_TEST_FORCE_LOAD_FORMAT=auto pytest -v -s quantization/ --ignore quantization/test_blackwell_moe.py

#-----------------------------------------------------------  mi300 · rocm  ------------------------------------------------------------#

- label: ROCm AITER Ops Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s rocm/aiter/

#---------------------------------------------------------  mi300 · samplers  ----------------------------------------------------------#

- label: Samplers Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s samplers

#------------------------------------------------------------  mi300 · misc  ------------------------------------------------------------#

- label: Python-only Installation # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash standalone_tests/python_only_compile.sh

- label: Regression # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install modelscope
  - pytest -v -s test_regression.py

#---------------------------------------------------------  mi300 · ray_compat  ---------------------------------------------------------#

- label: Ray Dependency Compatibility Check # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/"
  commands:
  - bash /vllm-workspace/.buildkite/scripts/check-ray-compatibility.sh

#------------------------------------------------------------  mi300 · v1  -------------------------------------------------------------#

- label: Acceptance Length Test (Large Models) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_ALLOW_INSECURE_SERIALIZATION=1
  - pytest -v -s v1/spec_decode/test_acceptance_length.py -m slow_test

- label: e2e Scheduling (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/general/test_async_scheduling.py

- label: Engine (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/engine/test_preprocess_error_handling.py
  - pytest -v -s v1/engine --ignore v1/engine/test_preprocess_error_handling.py

- label: Spec Decode Draft Model # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "draft_model or no_sync or batch_inference"

- label: Spec Decode Eagle # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "eagle_correctness"

- label: Spec Decode Ngram + Suffix # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "ngram or suffix"

- label: Spec Decode Speculators + MTP # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "speculators or mtp_correctness"

- label: Speculators Correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_ALLOW_INSECURE_SERIALIZATION=1
  - pytest -v -s v1/spec_decode/test_speculators_correctness.py -m slow_test

- label: Extract Hidden States Integration # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s v1/kv_connector/extract_hidden_states_integration

- label: V1 attention (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/attention

- label: V1 Core + KV + Metrics # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - pytest -v -s -m 'not cpu_test' v1/core
  - pytest -v -s v1/executor
  - pytest -v -s v1/kv_offload
  - pytest -v -s v1/worker
  - pytest -v -s -m 'not cpu_test' v1/kv_connector/unit
  - pytest -v -s -m 'not cpu_test' v1/metrics
  - pip install -U git+https://github.com/robertgshaw2-redhat/lm-evaluation-harness.git@streaming-api
  # - export HSA_NO_SCRATCH_RECLAIM=1
  - pytest -v -s entrypoints/openai/correctness/test_lmeval.py::test_lm_eval_accuracy_v1_engine

- label: V1 others (CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s -m 'cpu_test' v1/core
  - pytest -v -s v1/structured_output
  - pytest -v -s v1/test_serial_utils.py
  - pytest -v -s -m 'cpu_test' v1/kv_connector/unit
  - pytest -v -s -m 'cpu_test' v1/metrics

- label: V1 Sample + Logits # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/sample
  - pytest -v -s v1/logits_processors
  - pytest -v -s v1/test_oracle.py
  - pytest -v -s v1/test_request.py
  - pytest -v -s v1/test_outputs.py

- label: Distributed DP Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_async_llm_dp.py
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_eagle_dp.py
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_external_lb_dp.py
  - DP_SIZE=2 pytest -v -s entrypoints/openai/test_multi_api_servers.py

- label: Metrics, Tracing (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - "pip install \
      'opentelemetry-sdk>=1.26.0' \
      'opentelemetry-api>=1.26.0' \
      'opentelemetry-exporter-otlp>=1.26.0' \
      'opentelemetry-semantic-conventions-ai>=0.4.1'"
  - pytest -v -s v1/tracing

- label: V1 e2e (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s v1/e2e/spec_decode/test_spec_decode.py -k "tensor_parallelism"

- label: NixlConnector PD + Spec Decode acceptance (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_spec_decode_test.sh

- label: CrossLayer KV layout Distributed NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - CROSS_LAYERS_BLOCKS=True ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: Distributed DP Tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_async_llm_dp.py
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_eagle_dp.py
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_external_lb_dp.py
  - TP_SIZE=1 DP_SIZE=4 pytest -v -s v1/distributed/test_internal_lb_dp.py
  - TP_SIZE=1 DP_SIZE=4 pytest -v -s v1/distributed/test_hybrid_lb_dp.py
  - pytest -v -s v1/engine/test_engine_core_client.py::test_kv_cache_events_dp
  - pytest -v -s distributed/test_utils.py

- label: Distributed NixlConnector PD accuracy (4 GPUs)  # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: DP EP Distributed NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - DP_EP=1 ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: Hybrid SSM NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - HYBRID_SSM=1 ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: V1 e2e (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s v1/e2e/spec_decode/test_spec_decode.py -k "eagle_correctness_heavy"

- label: V1 e2e (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  commands:
    - pytest -v -s v1/e2e/test_hybrid_chunked_prefill.py

#------------------------------------------------------  mi300 · weight_loading  -------------------------------------------------------#

- label: Weight Loading Multiple GPU # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash weight_loading/run_model_weight_loading_test.sh -c weight_loading/models-amd.txt

- label: Weight Loading Multiple GPU - Large Models # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash weight_loading/run_model_weight_loading_test.sh -c weight_loading/models-large-amd.txt

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#----------------------------------------------------------  mi300 · compile  ----------------------------------------------------------#

- label: Distributed Compile + RPC Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s entrypoints/llm/test_collective_rpc.py
  - pytest -v -s ./compile/fullgraph/test_basic_correctness.py
  - pytest -v -s ./compile/test_wrapper.py

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Distributed Torchrun + Shutdown Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - VLLM_TEST_SAME_HOST=1 torchrun --nproc-per-node=4 distributed/test_same_node.py | grep 'Same node test passed'
  - VLLM_TEST_SAME_HOST=1 VLLM_TEST_WITH_DEFAULT_DEVICE_SET=1 torchrun --nproc-per-node=4 distributed/test_same_node.py | grep 'Same node test passed'
  - CUDA_VISIBLE_DEVICES=0,1 pytest -v -s v1/shutdown
  - pytest -v -s v1/worker/test_worker_memory_snapshot.py

- label: Distributed Compile + Comm (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s compile/fullgraph/test_basic_correctness.py
  - pytest -v -s distributed/test_pynccl.py
  - pytest -v -s distributed/test_events.py
  - pytest -v -s distributed/test_symm_mem_allreduce.py
  - pytest -v -s distributed/test_multiproc_executor.py::test_multiproc_executor_multi_node

#----------------------------------------------------------  mi300 · engine  -----------------------------------------------------------#

- label: Engine # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s engine test_sequence.py test_config.py test_logger.py test_vllm_port.py test_jit_monitor.py

#-----------------------------------------------------------  mi300 · evals  -----------------------------------------------------------#

- label: LM Eval Large Models (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_USE_DEEP_GEMM=0
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large-rocm-fp8.txt --tp-size=4

- label: ROCm LM Eval Large Models (8 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large-rocm.txt --tp-size=8

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: Language Models Test (Extended Generation) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system --no-build-isolation 'git+https://github.com/AndreasKaratzas/mamba@fix-rocm-7.0-warp-size-constexpr'
  - uv pip install --system --no-build-isolation 'git+https://github.com/Dao-AILab/causal-conv1d@v1.6.0'
  - pytest -v -s models/language/generation -m '(not core_model) and (not hybrid_model)'

- label: Language Models Tests (Hybrid) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system --no-build-isolation 'git+https://github.com/AndreasKaratzas/mamba@fix-rocm-7.0-warp-size-constexpr'
  - uv pip install --system --no-build-isolation 'git+https://github.com/Dao-AILab/causal-conv1d@v1.6.0'
  - pytest -v -s models/language/generation -m hybrid_model --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#----------------------------------------------------  mi300 · models / multimodal  ----------------------------------------------------#

- label: Multi-Modal Models (Extended Pooling) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/multimodal/pooling -m 'not core_model'

- label: "Multi-Modal Models (Standard) 2: qwen3 + gemma" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "qwen3 or gemma"
  - pytest -v -s models/multimodal/generation/test_qwen2_5_vl.py -m core_model

#------------------------------------------------------------  mi300 · v1  -------------------------------------------------------------#

- label: V1 Spec Decode # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s -m 'not slow_test' v1/spec_decode

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Distributed Quick Reduce Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - pytest -v -s tests/distributed/test_rocm_quick_reduce.py
  - pytest -v -s tests/distributed/test_quick_all_reduce.py

- label: Language Models Test (PPL) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/generation_ppl_test

### Copy B ###

- label: Copy-B PyTorch Compilation Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - "find compile/ -maxdepth 1 -name 'test_*.py' -print0 | xargs -0 -n1 -I{} pytest -s -v '{}'"

- label: Copy-B PyTorch Fullgraph Smoke Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - "find compile/fullgraph/ -name 'test_*.py' -not -name 'test_full_graph.py' -exec pytest -s -v {} \\\\;"

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Copy-B Pipeline + Context Parallelism (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_pp_cudagraph.py
  - pytest -v -s distributed/test_pipeline_parallel.py

#----------------------------------------------------------  mi300 · kernels  ----------------------------------------------------------#

- label: Copy-B Kernels Helion Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install helion==1.1.0
  - pytest -v -s kernels/helion/

- label: Copy-B Kernels Mamba Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/mamba

#------------------------------------------------------  mi300 · models / basic  -------------------------------------------------------#

- label: Copy-B Basic Models Test (Other CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_utils.py models/test_vision.py

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: Copy-B Language Models Test (MTEB) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/pooling_mteb_test

- label: Copy-B Batch Invariance (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pip install pytest-timeout pytest-forked
  - pytest -v -s v1/determinism/test_batch_invariance.py
  - pytest -v -s v1/determinism/test_rms_norm_batch_invariant.py

- label: Copy-B Cudagraph # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/cudagraph/test_cudagraph_dispatch.py
  - pytest -v -s v1/cudagraph/test_cudagraph_mode.py

- label: Copy-B e2e Core (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/general --ignore v1/e2e/general/test_async_scheduling.py

- label: Copy-B Async Engine, Inputs, Utils, Worker, Config (CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - python3 standalone_tests/lazy_imports.py
  - pytest -v -s test_inputs.py
  - pytest -v -s test_outputs.py
  - pytest -v -s test_pooling_params.py
  - pytest -v -s test_ray_env.py
  - pytest -v -s -m 'cpu_test' multimodal
  - pytest -v -s renderers
  - pytest -v -s tokenizers_
  - pytest -v -s reasoning
  - pytest -v -s tool_parsers
  - pytest -v -s parser
  - pytest -v -s transformers_utils
  - pytest -v -s config

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#-----------------------------------------------------  mi300 · basic_correctness  -----------------------------------------------------#

- label: Copy-B Basic Correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s basic_correctness/test_mem.py
  - VLLM_TARGET_TEST_SUITE=MI300 pytest -v -s basic_correctness/test_basic_correctness.py
  - pytest -v -s basic_correctness/test_cpu_offload.py

- label: Copy-B Distributed Model Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - TARGET_TEST_SUITE=MI300 pytest basic_correctness/ -v -s -m 'distributed(num_gpus=2)'
  - CUDA_VISIBLE_DEVICES=0,1 pytest -v -s model_executor/model_loader/test_sharded_state_loader.py -m '(not slow_test)'
  - pytest models/test_transformers.py -v -s -m 'distributed(num_gpus=2)'
  - pytest models/language -v -s -m 'distributed(num_gpus=2)'
  - pytest models/multimodal -v -s -m 'distributed(num_gpus=2)' --ignore models/multimodal/generation/test_whisper.py --ignore models/multimodal/generation/test_phi4siglip.py
  - pytest models/multimodal/generation/test_phi4siglip.py -v -s -m 'distributed(num_gpus=2)'
  - VLLM_WORKER_MULTIPROC_METHOD=spawn pytest models/multimodal/generation/test_whisper.py -v -s -m 'distributed(num_gpus=2)'

#--------------------------------------------------------  mi300 · benchmarks  ---------------------------------------------------------#

- label: Copy-B Benchmarks CLI Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s benchmarks/

#----------------------------------------------------------  mi300 · compile  ----------------------------------------------------------#

- label: Copy-B Fusion E2E Config Sweep (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace/"
  commands:
  - rocm-smi
  - pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k "llama-3"

- label: Copy-B Fusion E2E Quick (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace/"
  commands:
  - rocm-smi
  # Run all models and attn backends but only Inductor partition and native custom ops
  - "pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k 'inductor_partition and not +rms_norm and not +quant_fp8'"
  # Different from CUDA, Qwen requires +rms_norm and +quant_fp8 as rms+quant fusion is only supported on AITER
  - "pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k 'inductor_partition and +rms_norm and +quant_fp8 and qwen3'"

- label: Copy-B PyTorch Compilation Passes Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -s -v compile/passes --ignore compile/passes/distributed

- label: Copy-B PyTorch Fullgraph # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s compile/fullgraph/test_full_graph.py -k 'not test_fp8_kv_scale_compile'

- label: Copy-B Pytorch Nightly Dependency Override Check # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  soft_fail: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash standalone_tests/pytorch_nightly_dependency.sh

- label: Copy-B Distributed Compile Unit Tests (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - export VLLM_TEST_CLEAN_GPU_MEMORY=1
  - VLLM_TEST_CLEAN_GPU_MEMORY=1 pytest -v -s tests/compile/passes/distributed/test_async_tp.py
  - pytest -v -s tests/compile/fusions_e2e/test_tp2_ar_rms.py::test_tp2_ar_rms_fusions

#-----------------------------------------------------------  mi300 · cuda  ------------------------------------------------------------#

- label: Copy-B Platform Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s cuda/test_cuda_context.py
  - pytest -v -s cuda/test_platform_no_cuda_init.py

#--------------------------------------------------------  mi300 · detokenizer  --------------------------------------------------------#

- label: Copy-B Async Engine, Inputs, Utils, Worker # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s detokenizer
  - pytest -v -s -m 'not cpu_test' multimodal
  - pytest -v -s utils_

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Copy-B Distributed Comm Ops # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_comm_ops.py
  - pytest -v -s distributed/test_shm_broadcast.py
  - pytest -v -s distributed/test_shm_buffer.py
  - pytest -v -s distributed/test_shm_storage.py

- label: Copy-B EPLB Algorithm # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_eplb_algo.py
  - pytest -v -s distributed/test_eplb_utils.py

- label: Copy-B EPLB Execution # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_eplb_execute.py
  - pytest -v -s distributed/test_eplb_spec_decode.py

- label: Copy-B Distributed Tests (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - pytest -v -s tests/distributed/test_context_parallel.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 examples/rl/rlhf_async_new_apis.py
  - VLLM_LOGGING_LEVEL=DEBUG python3 examples/features/data_parallel/data_parallel_offline.py --model=Qwen/Qwen1.5-MoE-A2.7B -tp=1 -dp=2 --max-model-len=2048 --all2all-backend=deepep_high_throughput
  - VLLM_LOGGING_LEVEL=DEBUG python3 examples/features/data_parallel/data_parallel_offline.py --model=Qwen/Qwen1.5-MoE-A2.7B -tp=1 -dp=2 --max-model-len=2048 --all2all-backend=allgather_reducescatter --disable-nccl-for-dp-synchronization
  - pytest -v -s tests/v1/distributed/test_dbo.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 pytest -v -s tests/distributed/test_weight_transfer.py
  - pytest -v -s tests/distributed/test_packed_tensor.py

- label: Copy-B Distributed Tests (4xA100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_custom_all_reduce.py
  - torchrun --nproc_per_node=2 distributed/test_ca_buffer_sharing.py
  - TARGET_TEST_SUITE=MI300 pytest basic_correctness/ -v -s -m 'distributed(num_gpus=2)'
  - pytest -v -s -x lora/test_mixtral.py

- label: Copy-B Distributed Torchrun + Examples (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - torchrun --nproc-per-node=4 distributed/test_torchrun_example.py
  - PP_SIZE=2 torchrun --nproc-per-node=4 distributed/test_torchrun_example.py
  - TP_SIZE=4 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - PP_SIZE=2 TP_SIZE=2 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - DP_SIZE=4 ENABLE_EP=1 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - TP_SIZE=2 DP_SIZE=2 ENABLE_EP=1 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - python3 ../examples/features/data_parallel/data_parallel_offline.py --enforce-eager
  # rlhf examples
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 ../examples/rl/rlhf_nccl.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 ../examples/rl/rlhf_ipc.py

- label: Copy-B Elastic EP Scaling Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_elastic_ep.py

- label: Copy-B RayExecutorV2 (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_USE_RAY_V2_EXECUTOR_BACKEND=1
  - pytest -v -s distributed/test_ray_v2_executor.py
  - pytest -v -s distributed/test_ray_v2_executor_e2e.py
  - pytest -v -s distributed/test_pipeline_parallel.py -k "ray"
  - TARGET_TEST_SUITE=L4 pytest -v -s basic_correctness/test_basic_correctness.py -k "ray"

- label: Copy-B Distributed Tests (8xH100-8xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/tests"
  commands:
  - torchrun --nproc-per-node=8 ../examples/features/torchrun/torchrun_dp_example_offline.py --tp-size=2 --pp-size=1 --dp-size=4 --enable-ep

#--------------------------------------------------------  mi300 · entrypoints  --------------------------------------------------------#

- label: Copy-B Entrypoints Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s entrypoints/unit_tests
  - pytest -v -s entrypoints/weight_transfer

- label: Copy-B Entrypoints Integration (LLM) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/llm --ignore=entrypoints/llm/test_generate.py --ignore=entrypoints/llm/test_collective_rpc.py --ignore=entrypoints/llm/offline_mode
  - pytest -v -s entrypoints/llm/test_generate.py # it needs a clean process
  - pytest -v -s entrypoints/llm/offline_mode # Needs to avoid interference with other tests

- label: Copy-B Entrypoints Integration (API Server) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/serve --ignore=entrypoints/serve/dev/rpc
  - PYTHONPATH=/vllm-workspace pytest -v -s entrypoints/serve/dev/rpc

- label: Copy-B Entrypoints Integration (API Server OpenAI - Part 1) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai --ignore=entrypoints/openai/completion --ignore=entrypoints/openai/chat_completion --ignore=entrypoints/openai/responses --ignore=entrypoints/openai/correctness

- label: Copy-B Entrypoints Integration (API Server OpenAI - Part 2) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai/chat_completion
  - pytest -v -s entrypoints/openai/completion --ignore=entrypoints/openai/completion/test_tensorizer_entrypoint.py

- label: Copy-B Entrypoints Integration (API Server Generate) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s tool_use
  - pytest -v -s entrypoints/tool_parsers
  - pytest -v -s entrypoints/generate
  - pytest -v -s entrypoints/anthropic

- label: Copy-B Entrypoints Integration (Responses API) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai/responses

- label: Copy-B Entrypoints Integration (Speech to Text) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/speech_to_text

- label: Copy-B Entrypoints Integration (Multimodal)
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/multimodal

- label: Copy-B Entrypoints Integration (Pooling) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/pooling

- label: Copy-B OpenAI API correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash ../tools/install_torchcodec_rocm.sh || exit 1
  - pytest -s entrypoints/openai/correctness/

#-----------------------------------------------------------  mi300 · evals  -----------------------------------------------------------#

- label: Copy-B DeepSeek V2-Lite Prefetch Offload Accuracy (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/deepseek_v2_lite_prefetch_offload.sh 0.25 200 8030

- label: Copy-B LM Eval Small Models # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -s -v evals/gsm8k/test_gsm8k_correctness.py --config-list-file=configs/models-small.txt

- label: Copy-B LM Eval Small Models (MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-small-rocm.txt

- label: Copy-B Multi-Modal Accuracy Eval (Small Models) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-mm-small.txt --tp-size=1

- label: Copy-B GPQA Eval (GPT-OSS) (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
    - uv pip install --system 'gpt-oss[eval]==0.0.5'
    - pytest -s -v evals/gpt_oss/test_gpqa_correctness.py --config-list-file=configs/models-gfx942.txt

- label: Copy-B DeepSeek V2-Lite Sync EPLB Accuracy (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/deepseek_v2_lite_ep_eplb.sh 0.25 200 8010

- label: Copy-B LM Eval Large Models (4xA100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large.txt --tp-size=4

- label: Copy-B Qwen3-30B-A3B-FP8-block Sync EPLB Accuracy (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/qwen30b_a3b_fp8_block_ep_eplb.sh 0.8 200 8020

- label: Copy-B Qwen3-Next-80B-A3B-Instruct MTP Async EPLB Accuracy # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/qwen3_next_mtp_async_eplb.sh 0.8 1319 8040

- label: Copy-B LM Eval Large Models (8xH200-8xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v evals/gsm8k/test_gsm8k_correctness.py --config-list-file=configs/models-mi3xx.txt

#---------------------------------------------------------  mi300 · examples  ----------------------------------------------------------#

- label: Copy-B Examples # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/examples"
  commands:
    - pip install tensorizer
    # Basic
    - python3 basic/offline_inference/chat.py
    - python3 basic/offline_inference/generate.py --model facebook/opt-125m
    - python3 basic/offline_inference/generate.py --model meta-llama/Llama-2-13b-chat-hf --cpu-offload-gb 10
    - python3 basic/offline_inference/classify.py
    - python3 basic/offline_inference/embed.py
    - python3 basic/offline_inference/score.py
    # Multi-modal models
    - python3 generate/multimodal/audio_language_offline.py --seed 0
    - python3 generate/multimodal/vision_language_offline.py --seed 0
    - python3 generate/multimodal/vision_language_multi_image_offline.py --seed 0
    - python3 generate/multimodal/encoder_decoder_multimodal_offline.py --model-type whisper --seed 0
    # Pooling models
    - python3 pooling/embed/vision_embedding_offline.py --seed 0
    # Features demo
    - python3 features/automatic_prefix_caching/prefix_caching_offline.py
    - python3 deployment/llm_engine_example.py
    - python3 features/tensorize_vllm_model.py --model facebook/opt-125m serialize --serialized-directory /tmp/ --suffix v1 && python3 features/tensorize_vllm_model.py --model facebook/opt-125m deserialize --path-to-tensors /tmp/vllm/facebook/opt-125m/v1/model.tensors
    - python3 features/speculative_decoding/spec_decode_offline.py --test --method eagle --num_spec_tokens 3 --dataset-name hf --dataset-path philschmid/mt-bench --num-prompts 80 --temp 0 --top-p 1.0 --top-k -1 --tp 1 --enable-chunked-prefill --max-model-len 2048
    - python3 features/speculative_decoding/spec_decode_offline.py --test --method eagle3 --num_spec_tokens 3 --dataset-name hf --dataset-path philschmid/mt-bench --num-prompts 80 --temp 0 --top-p 1.0 --top-k -1 --tp 1 --enable-chunked-prefill --max-model-len 1536

#----------------------------------------------------------  mi300 · kernels  ----------------------------------------------------------#

- label: Copy-B Kernels Attention Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/attention --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: Copy-B Kernels Core Operation Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/core --ignore=kernels/core/test_minimax_reduce_rms.py  kernels/test_concat_mla_q.py kernels/test_top_k_per_row.py

- label: Copy-B Kernels MoE Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 5
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/moe --ignore=kernels/moe/test_modular_oai_triton_moe.py --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT
  - pytest -v -s kernels/moe/test_modular_oai_triton_moe.py --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: Copy-B Kernels Quantization Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/quantization --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: Copy-B Kernels FP8 MoE Test (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s kernels/moe/test_deepep_moe.py

#-----------------------------------------------------------  mi300 · lora  ------------------------------------------------------------#

- label: Copy-B LoRA %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s lora --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --ignore=lora/test_chatglm3_tp.py --ignore=lora/test_llama_tp.py --ignore=lora/test_qwen3_with_multi_loras.py --ignore=lora/test_olmoe_tp.py --ignore=lora/test_deepseekv2_tp.py --ignore=lora/test_gptoss_tp.py --ignore=lora/test_qwen3moe_tp.py --ignore=lora/test_qwen35_densemodel_lora.py

- label: Copy-B LoRA TP (Distributed) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
  - pytest -v -s -x lora/test_chatglm3_tp.py
  - pytest -v -s -x lora/test_llama_tp.py
  - pytest -v -s -x lora/test_qwen3_with_multi_loras.py
  - pytest -v -s -x lora/test_olmoe_tp.py
  - pytest -v -s -x lora/test_gptoss_tp.py
  - pytest -v -s -x lora/test_qwen35_densemodel_lora.py

#------------------------------------------------------  mi300 · model_executor  -------------------------------------------------------#

- label: Copy-B Model Executor # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - apt-get update && apt-get install -y curl libsodium23
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s model_executor -m '(not slow_test)'
  - pytest -v -s entrypoints/openai/completion/test_tensorizer_entrypoint.py

#------------------------------------------------------  mi300 · models / basic  -------------------------------------------------------#

- label: Copy-B Basic Models Tests (Extra Initialization) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_initialization.py -k 'not test_can_initialize_small_subset' --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

- label: Copy-B Basic Models Tests (Initialization) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_initialization.py::test_can_initialize_small_subset

- label: Copy-B Basic Models Tests (Other) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_terratorch.py models/test_transformers.py models/test_registry.py

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: Copy-B Language Models Test (Extended Pooling)  # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/pooling -m 'not core_model'

- label: Copy-B Language Models Tests (Standard) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip freeze | grep -E 'torch'
  - pytest -v -s models/language -m 'core_model and (not slow_test)'

- label: Copy-B Language Models Tests (Extra Standard) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip freeze | grep -E 'torch'
  - pytest -v -s models/language -m 'core_model and slow_test' --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#----------------------------------------------------  mi300 · models / multimodal  ----------------------------------------------------#

- label: Copy-B Multi-Modal Models (Extended Generation 1) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation -m 'not core_model' --ignore models/multimodal/generation/test_common.py
  - pytest -v -s models/multimodal/test_mapping.py

- label: Copy-B Multi-Modal Models (Extended Generation 2) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m 'split(group=0) and not core_model'


- label: Copy-B Multi-Modal Models (Extended Generation 3) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m 'split(group=1) and not core_model'

- label: "Copy-B Multi-Modal Models (Standard) 1: qwen2" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "qwen2"
  - pytest -v -s models/multimodal/generation/test_ultravox.py -m core_model

- label: "Copy-B Multi-Modal Models (Standard) 3: llava + qwen2_vl" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "not qwen2 and not qwen3 and not gemma"
  - pytest -v -s models/multimodal/generation/test_qwen2_vl.py -m core_model

- label: "Copy-B Multi-Modal Models (Standard) 4: other + whisper" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal -m core_model --ignore models/multimodal/generation/test_common.py --ignore models/multimodal/generation/test_ultravox.py --ignore models/multimodal/generation/test_qwen2_5_vl.py --ignore models/multimodal/generation/test_qwen2_vl.py --ignore models/multimodal/generation/test_whisper.py  --ignore models/multimodal/generation/test_memory_leak.py --ignore models/multimodal/processing
  - pytest -v -s models/multimodal/generation/test_memory_leak.py -m core_model
  - cd .. && VLLM_WORKER_MULTIPROC_METHOD=spawn pytest -v -s tests/models/multimodal/generation/test_whisper.py -m core_model

- label: Copy-B Multi-Modal Processor # 1h 42m
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/processing/test_tensor_schema.py

- label: Copy-B Multi-Modal Processor (CPU) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/processing --ignore models/multimodal/processing/test_tensor_schema.py --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#-----------------------------------------------------  mi300 · models / quantized  -----------------------------------------------------#

- label: Copy-B Quantized Models Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/quantization

#--------------------------------------------------  mi300 · models / transformers  ---------------------------------------------------#

- label: Copy-B Transformers Nightly Models (Shardable) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  working_dir: "/vllm-workspace/"
  commands:
  - pip install --upgrade git+https://github.com/huggingface/transformers
  - pytest -v -s tests/models/test_initialization.py --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB
  - pytest -v -s tests/models/multimodal/processing/ --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

- label: Copy-B Transformers Nightly Models (Single) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/"
  commands:
  - pip install --upgrade git+https://github.com/huggingface/transformers
  - pytest -v -s tests/models/test_transformers.py
  - pytest -v -s tests/models/multimodal/test_mapping.py
  - python3 examples/basic/offline_inference/chat.py
  - python3 examples/generate/multimodal/vision_language_offline.py --model-type qwen2_5_vl
  - VLLM_WORKER_MULTIPROC_METHOD=spawn python3 examples/generate/multimodal/audio_language_offline.py --model-type whisper

#----------------------------------------------------------  mi300 · plugins  ----------------------------------------------------------#

- label: Copy-B Plugin Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  # BEGIN: platform plugin and general plugin tests, all the code in-between runs on dummy platform
  - pip install -e ./plugins/vllm_add_dummy_platform
  - pytest -v -s plugins_tests/test_platform_plugins.py
  - pip uninstall vllm_add_dummy_platform -y
  # END: platform plugin tests
  # BEGIN: `io_processor` plugins test, all the code in between uses the `prithvi_io_processor` plugin
  - pip install -e ./plugins/prithvi_io_processor_plugin
  - pytest -v -s plugins_tests/test_io_processor_plugins.py
  - pytest -v -s plugins_tests/test_terratorch_io_processor_plugins.py
  - pip uninstall prithvi_io_processor_plugin -y
  # END: `io_processor` plugins test
  # BEGIN: `bge_m3_sparse io_processor` test
  - pip install -e ./plugins/bge_m3_sparse_plugin
  - pytest -v -s plugins_tests/test_bge_m3_sparse_io_processor_plugins.py
  - pip uninstall bge_m3_sparse_plugin -y
  # END: `bge_m3_sparse io_processor` test
  # BEGIN: `colbert_query io_processor` test
  - pip install -e ./plugins/colbert_query_plugin
  - pytest -v -s plugins_tests/test_colbert_query_io_processor_plugins.py
  - pip uninstall colbert_query_plugin -y
  # END: `colbert_query io_processor` test
  # BEGIN: `stat_logger` plugins test
  - pip install -e ./plugins/vllm_add_dummy_stat_logger
  - pytest -v -s plugins_tests/test_stats_logger_plugins.py
  - pip uninstall dummy_stat_logger -y
  # END: `stat_logger` plugins test
  # BEGIN: other tests
  - pytest -v -s plugins_tests/test_scheduler_plugins.py
  - pip install -e ./plugins/vllm_add_dummy_model
  - pytest -v -s distributed/test_distributed_oot.py
  - pytest -v -s plugins_tests/test_oot_registration_online.py # it needs a clean process
  - pytest -v -s plugins_tests/test_oot_registration_offline.py # it needs a clean process
  - pytest -v -s plugins_tests/lora_resolvers # unit tests for in-tree lora resolver plugins

#-------------------------------------------------------  mi300 · quantization  --------------------------------------------------------#

- label: Copy-B Quantization # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:

  # temporary install here since we need nightly, will move to requirements/test.in
  # after torchao 0.12 release, and pin a working version of torchao nightly here

  # since torchao nightly is only compatible with torch nightly currently
  # https://github.com/pytorch/ao/issues/2919, we'll have to skip new torchao tests for now
  # we can only upgrade after this is resolved
  # TODO(jerryzh168): resolve the above comment
  - uv pip install --system torchao==0.17.0
  - uv pip install --system conch-triton-kernels
  - VLLM_TEST_FORCE_LOAD_FORMAT=auto pytest -v -s quantization/ --ignore quantization/test_blackwell_moe.py

#-----------------------------------------------------------  mi300 · rocm  ------------------------------------------------------------#

- label: Copy-B ROCm AITER Ops Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s rocm/aiter/

#---------------------------------------------------------  mi300 · samplers  ----------------------------------------------------------#

- label: Copy-B Samplers Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s samplers

#------------------------------------------------------------  mi300 · misc  ------------------------------------------------------------#

- label: Copy-B Python-only Installation # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash standalone_tests/python_only_compile.sh

- label: Copy-B Regression # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install modelscope
  - pytest -v -s test_regression.py

#---------------------------------------------------------  mi300 · ray_compat  ---------------------------------------------------------#

- label: Copy-B Ray Dependency Compatibility Check # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/"
  commands:
  - bash /vllm-workspace/.buildkite/scripts/check-ray-compatibility.sh

#------------------------------------------------------------  mi300 · v1  -------------------------------------------------------------#

- label: Copy-B Acceptance Length Test (Large Models) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_ALLOW_INSECURE_SERIALIZATION=1
  - pytest -v -s v1/spec_decode/test_acceptance_length.py -m slow_test

- label: Copy-B e2e Scheduling (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/general/test_async_scheduling.py

- label: Copy-B Engine (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/engine/test_preprocess_error_handling.py
  - pytest -v -s v1/engine --ignore v1/engine/test_preprocess_error_handling.py

- label: Copy-B Spec Decode Draft Model # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "draft_model or no_sync or batch_inference"

- label: Copy-B Spec Decode Eagle # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "eagle_correctness"

- label: Copy-B Spec Decode Ngram + Suffix # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "ngram or suffix"

- label: Copy-B Spec Decode Speculators + MTP # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "speculators or mtp_correctness"

- label: Copy-B Speculators Correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_ALLOW_INSECURE_SERIALIZATION=1
  - pytest -v -s v1/spec_decode/test_speculators_correctness.py -m slow_test

- label: Copy-B Extract Hidden States Integration # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s v1/kv_connector/extract_hidden_states_integration

- label: Copy-B V1 attention (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/attention

- label: Copy-B V1 Core + KV + Metrics # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - pytest -v -s -m 'not cpu_test' v1/core
  - pytest -v -s v1/executor
  - pytest -v -s v1/kv_offload
  - pytest -v -s v1/worker
  - pytest -v -s -m 'not cpu_test' v1/kv_connector/unit
  - pytest -v -s -m 'not cpu_test' v1/metrics
  - pip install -U git+https://github.com/robertgshaw2-redhat/lm-evaluation-harness.git@streaming-api
  # - export HSA_NO_SCRATCH_RECLAIM=1
  - pytest -v -s entrypoints/openai/correctness/test_lmeval.py::test_lm_eval_accuracy_v1_engine

- label: Copy-B V1 others (CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s -m 'cpu_test' v1/core
  - pytest -v -s v1/structured_output
  - pytest -v -s v1/test_serial_utils.py
  - pytest -v -s -m 'cpu_test' v1/kv_connector/unit
  - pytest -v -s -m 'cpu_test' v1/metrics

- label: Copy-B V1 Sample + Logits # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/sample
  - pytest -v -s v1/logits_processors
  - pytest -v -s v1/test_oracle.py
  - pytest -v -s v1/test_request.py
  - pytest -v -s v1/test_outputs.py

- label: Copy-B Distributed DP Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_async_llm_dp.py
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_eagle_dp.py
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_external_lb_dp.py
  - DP_SIZE=2 pytest -v -s entrypoints/openai/test_multi_api_servers.py

- label: Copy-B Metrics, Tracing (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - "pip install \
      'opentelemetry-sdk>=1.26.0' \
      'opentelemetry-api>=1.26.0' \
      'opentelemetry-exporter-otlp>=1.26.0' \
      'opentelemetry-semantic-conventions-ai>=0.4.1'"
  - pytest -v -s v1/tracing

- label: Copy-B V1 e2e (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s v1/e2e/spec_decode/test_spec_decode.py -k "tensor_parallelism"

- label: Copy-B NixlConnector PD + Spec Decode acceptance (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_spec_decode_test.sh

- label: Copy-B CrossLayer KV layout Distributed NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - CROSS_LAYERS_BLOCKS=True ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: Copy-B Distributed DP Tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_async_llm_dp.py
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_eagle_dp.py
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_external_lb_dp.py
  - TP_SIZE=1 DP_SIZE=4 pytest -v -s v1/distributed/test_internal_lb_dp.py
  - TP_SIZE=1 DP_SIZE=4 pytest -v -s v1/distributed/test_hybrid_lb_dp.py
  - pytest -v -s v1/engine/test_engine_core_client.py::test_kv_cache_events_dp
  - pytest -v -s distributed/test_utils.py

- label: Copy-B Distributed NixlConnector PD accuracy (4 GPUs)  # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: Copy-B DP EP Distributed NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - DP_EP=1 ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: Copy-B Hybrid SSM NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - HYBRID_SSM=1 ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: Copy-B V1 e2e (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s v1/e2e/spec_decode/test_spec_decode.py -k "eagle_correctness_heavy"

- label: Copy-B V1 e2e (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  commands:
    - pytest -v -s v1/e2e/test_hybrid_chunked_prefill.py

#------------------------------------------------------  mi300 · weight_loading  -------------------------------------------------------#

- label: Copy-B Weight Loading Multiple GPU # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash weight_loading/run_model_weight_loading_test.sh -c weight_loading/models-amd.txt

- label: Copy-B Weight Loading Multiple GPU - Large Models # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash weight_loading/run_model_weight_loading_test.sh -c weight_loading/models-large-amd.txt

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#----------------------------------------------------------  mi300 · compile  ----------------------------------------------------------#

- label: Copy-B Distributed Compile + RPC Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s entrypoints/llm/test_collective_rpc.py
  - pytest -v -s ./compile/fullgraph/test_basic_correctness.py
  - pytest -v -s ./compile/test_wrapper.py

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Copy-B Distributed Torchrun + Shutdown Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - VLLM_TEST_SAME_HOST=1 torchrun --nproc-per-node=4 distributed/test_same_node.py | grep 'Same node test passed'
  - VLLM_TEST_SAME_HOST=1 VLLM_TEST_WITH_DEFAULT_DEVICE_SET=1 torchrun --nproc-per-node=4 distributed/test_same_node.py | grep 'Same node test passed'
  - CUDA_VISIBLE_DEVICES=0,1 pytest -v -s v1/shutdown
  - pytest -v -s v1/worker/test_worker_memory_snapshot.py

- label: Copy-B Distributed Compile + Comm (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s compile/fullgraph/test_basic_correctness.py
  - pytest -v -s distributed/test_pynccl.py
  - pytest -v -s distributed/test_events.py
  - pytest -v -s distributed/test_symm_mem_allreduce.py
  - pytest -v -s distributed/test_multiproc_executor.py::test_multiproc_executor_multi_node

#----------------------------------------------------------  mi300 · engine  -----------------------------------------------------------#

- label: Copy-B Engine # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s engine test_sequence.py test_config.py test_logger.py test_vllm_port.py test_jit_monitor.py

#-----------------------------------------------------------  mi300 · evals  -----------------------------------------------------------#

- label: Copy-B LM Eval Large Models (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_USE_DEEP_GEMM=0
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large-rocm-fp8.txt --tp-size=4

- label: Copy-B ROCm LM Eval Large Models (8 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large-rocm.txt --tp-size=8

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: Copy-B Language Models Test (Extended Generation) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system --no-build-isolation 'git+https://github.com/AndreasKaratzas/mamba@fix-rocm-7.0-warp-size-constexpr'
  - uv pip install --system --no-build-isolation 'git+https://github.com/Dao-AILab/causal-conv1d@v1.6.0'
  - pytest -v -s models/language/generation -m '(not core_model) and (not hybrid_model)'

- label: Copy-B Language Models Tests (Hybrid) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system --no-build-isolation 'git+https://github.com/AndreasKaratzas/mamba@fix-rocm-7.0-warp-size-constexpr'
  - uv pip install --system --no-build-isolation 'git+https://github.com/Dao-AILab/causal-conv1d@v1.6.0'
  - pytest -v -s models/language/generation -m hybrid_model --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#----------------------------------------------------  mi300 · models / multimodal  ----------------------------------------------------#

- label: Copy-B Multi-Modal Models (Extended Pooling) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/multimodal/pooling -m 'not core_model'

- label: "Copy-B Multi-Modal Models (Standard) 2: qwen3 + gemma" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "qwen3 or gemma"
  - pytest -v -s models/multimodal/generation/test_qwen2_5_vl.py -m core_model

#------------------------------------------------------------  mi300 · v1  -------------------------------------------------------------#

- label: Copy-B V1 Spec Decode # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s -m 'not slow_test' v1/spec_decode

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: Copy-B Distributed Quick Reduce Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - pytest -v -s tests/distributed/test_rocm_quick_reduce.py
  - pytest -v -s tests/distributed/test_quick_all_reduce.py

- label: Copy-B Language Models Test (PPL) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/generation_ppl_test

### Copy C ###

- label: COPY-C PyTorch Compilation Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - "find compile/ -maxdepth 1 -name 'test_*.py' -print0 | xargs -0 -n1 -I{} pytest -s -v '{}'"

- label: COPY-C PyTorch Fullgraph Smoke Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - "find compile/fullgraph/ -name 'test_*.py' -not -name 'test_full_graph.py' -exec pytest -s -v {} \\\\;"

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: COPY-C Pipeline + Context Parallelism (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_pp_cudagraph.py
  - pytest -v -s distributed/test_pipeline_parallel.py

#----------------------------------------------------------  mi300 · kernels  ----------------------------------------------------------#

- label: COPY-C Kernels Helion Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install helion==1.1.0
  - pytest -v -s kernels/helion/

- label: COPY-C Kernels Mamba Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/mamba

#------------------------------------------------------  mi300 · models / basic  -------------------------------------------------------#

- label: COPY-C Basic Models Test (Other CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_utils.py models/test_vision.py

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: COPY-C Language Models Test (MTEB) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/pooling_mteb_test

- label: COPY-C Batch Invariance (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pip install pytest-timeout pytest-forked
  - pytest -v -s v1/determinism/test_batch_invariance.py
  - pytest -v -s v1/determinism/test_rms_norm_batch_invariant.py

- label: COPY-C Cudagraph # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/cudagraph/test_cudagraph_dispatch.py
  - pytest -v -s v1/cudagraph/test_cudagraph_mode.py

- label: COPY-C e2e Core (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/general --ignore v1/e2e/general/test_async_scheduling.py

- label: COPY-C Async Engine, Inputs, Utils, Worker, Config (CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - python3 standalone_tests/lazy_imports.py
  - pytest -v -s test_inputs.py
  - pytest -v -s test_outputs.py
  - pytest -v -s test_pooling_params.py
  - pytest -v -s test_ray_env.py
  - pytest -v -s -m 'cpu_test' multimodal
  - pytest -v -s renderers
  - pytest -v -s tokenizers_
  - pytest -v -s reasoning
  - pytest -v -s tool_parsers
  - pytest -v -s parser
  - pytest -v -s transformers_utils
  - pytest -v -s config

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#-----------------------------------------------------  mi300 · basic_correctness  -----------------------------------------------------#

- label: COPY-C Basic Correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s basic_correctness/test_mem.py
  - VLLM_TARGET_TEST_SUITE=MI300 pytest -v -s basic_correctness/test_basic_correctness.py
  - pytest -v -s basic_correctness/test_cpu_offload.py

- label: COPY-C Distributed Model Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - TARGET_TEST_SUITE=MI300 pytest basic_correctness/ -v -s -m 'distributed(num_gpus=2)'
  - CUDA_VISIBLE_DEVICES=0,1 pytest -v -s model_executor/model_loader/test_sharded_state_loader.py -m '(not slow_test)'
  - pytest models/test_transformers.py -v -s -m 'distributed(num_gpus=2)'
  - pytest models/language -v -s -m 'distributed(num_gpus=2)'
  - pytest models/multimodal -v -s -m 'distributed(num_gpus=2)' --ignore models/multimodal/generation/test_whisper.py --ignore models/multimodal/generation/test_phi4siglip.py
  - pytest models/multimodal/generation/test_phi4siglip.py -v -s -m 'distributed(num_gpus=2)'
  - VLLM_WORKER_MULTIPROC_METHOD=spawn pytest models/multimodal/generation/test_whisper.py -v -s -m 'distributed(num_gpus=2)'

#--------------------------------------------------------  mi300 · benchmarks  ---------------------------------------------------------#

- label: COPY-C Benchmarks CLI Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s benchmarks/

#----------------------------------------------------------  mi300 · compile  ----------------------------------------------------------#

- label: COPY-C Fusion E2E Config Sweep (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace/"
  commands:
  - rocm-smi
  - pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k "llama-3"

- label: COPY-C Fusion E2E Quick (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace/"
  commands:
  - rocm-smi
  # Run all models and attn backends but only Inductor partition and native custom ops
  - "pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k 'inductor_partition and not +rms_norm and not +quant_fp8'"
  # Different from CUDA, Qwen requires +rms_norm and +quant_fp8 as rms+quant fusion is only supported on AITER
  - "pytest -v -s tests/compile/fusions_e2e/test_tp1_quant.py -k 'inductor_partition and +rms_norm and +quant_fp8 and qwen3'"

- label: COPY-C PyTorch Compilation Passes Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -s -v compile/passes --ignore compile/passes/distributed

- label: COPY-C PyTorch Fullgraph # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s compile/fullgraph/test_full_graph.py -k 'not test_fp8_kv_scale_compile'

- label: COPY-C Pytorch Nightly Dependency Override Check # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  soft_fail: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash standalone_tests/pytorch_nightly_dependency.sh

- label: COPY-C Distributed Compile Unit Tests (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - export VLLM_TEST_CLEAN_GPU_MEMORY=1
  - VLLM_TEST_CLEAN_GPU_MEMORY=1 pytest -v -s tests/compile/passes/distributed/test_async_tp.py
  - pytest -v -s tests/compile/fusions_e2e/test_tp2_ar_rms.py::test_tp2_ar_rms_fusions

#-----------------------------------------------------------  mi300 · cuda  ------------------------------------------------------------#

- label: COPY-C Platform Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s cuda/test_cuda_context.py
  - pytest -v -s cuda/test_platform_no_cuda_init.py

#--------------------------------------------------------  mi300 · detokenizer  --------------------------------------------------------#

- label: COPY-C Async Engine, Inputs, Utils, Worker # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s detokenizer
  - pytest -v -s -m 'not cpu_test' multimodal
  - pytest -v -s utils_

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: COPY-C Distributed Comm Ops # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_comm_ops.py
  - pytest -v -s distributed/test_shm_broadcast.py
  - pytest -v -s distributed/test_shm_buffer.py
  - pytest -v -s distributed/test_shm_storage.py

- label: COPY-C EPLB Algorithm # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_eplb_algo.py
  - pytest -v -s distributed/test_eplb_utils.py

- label: COPY-C EPLB Execution # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_eplb_execute.py
  - pytest -v -s distributed/test_eplb_spec_decode.py

- label: COPY-C Distributed Tests (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - pytest -v -s tests/distributed/test_context_parallel.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 examples/rl/rlhf_async_new_apis.py
  - VLLM_LOGGING_LEVEL=DEBUG python3 examples/features/data_parallel/data_parallel_offline.py --model=Qwen/Qwen1.5-MoE-A2.7B -tp=1 -dp=2 --max-model-len=2048 --all2all-backend=deepep_high_throughput
  - VLLM_LOGGING_LEVEL=DEBUG python3 examples/features/data_parallel/data_parallel_offline.py --model=Qwen/Qwen1.5-MoE-A2.7B -tp=1 -dp=2 --max-model-len=2048 --all2all-backend=allgather_reducescatter --disable-nccl-for-dp-synchronization
  - pytest -v -s tests/v1/distributed/test_dbo.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 pytest -v -s tests/distributed/test_weight_transfer.py
  - pytest -v -s tests/distributed/test_packed_tensor.py

- label: COPY-C Distributed Tests (4xA100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_custom_all_reduce.py
  - torchrun --nproc_per_node=2 distributed/test_ca_buffer_sharing.py
  - TARGET_TEST_SUITE=MI300 pytest basic_correctness/ -v -s -m 'distributed(num_gpus=2)'
  - pytest -v -s -x lora/test_mixtral.py

- label: COPY-C Distributed Torchrun + Examples (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - torchrun --nproc-per-node=4 distributed/test_torchrun_example.py
  - PP_SIZE=2 torchrun --nproc-per-node=4 distributed/test_torchrun_example.py
  - TP_SIZE=4 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - PP_SIZE=2 TP_SIZE=2 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - DP_SIZE=4 ENABLE_EP=1 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - TP_SIZE=2 DP_SIZE=2 ENABLE_EP=1 torchrun --nproc-per-node=4 distributed/test_torchrun_example_moe.py
  - python3 ../examples/features/data_parallel/data_parallel_offline.py --enforce-eager
  # rlhf examples
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 ../examples/rl/rlhf_nccl.py
  - VLLM_ALLOW_INSECURE_SERIALIZATION=1 python3 ../examples/rl/rlhf_ipc.py

- label: COPY-C Elastic EP Scaling Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s distributed/test_elastic_ep.py

- label: COPY-C RayExecutorV2 (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_USE_RAY_V2_EXECUTOR_BACKEND=1
  - pytest -v -s distributed/test_ray_v2_executor.py
  - pytest -v -s distributed/test_ray_v2_executor_e2e.py
  - pytest -v -s distributed/test_pipeline_parallel.py -k "ray"
  - TARGET_TEST_SUITE=L4 pytest -v -s basic_correctness/test_basic_correctness.py -k "ray"

- label: COPY-C Distributed Tests (8xH100-8xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/tests"
  commands:
  - torchrun --nproc-per-node=8 ../examples/features/torchrun/torchrun_dp_example_offline.py --tp-size=2 --pp-size=1 --dp-size=4 --enable-ep

#--------------------------------------------------------  mi300 · entrypoints  --------------------------------------------------------#

- label: COPY-C Entrypoints Unit Tests # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s entrypoints/unit_tests
  - pytest -v -s entrypoints/weight_transfer

- label: COPY-C Entrypoints Integration (LLM) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/llm --ignore=entrypoints/llm/test_generate.py --ignore=entrypoints/llm/test_collective_rpc.py --ignore=entrypoints/llm/offline_mode
  - pytest -v -s entrypoints/llm/test_generate.py # it needs a clean process
  - pytest -v -s entrypoints/llm/offline_mode # Needs to avoid interference with other tests

- label: COPY-C Entrypoints Integration (API Server) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/serve --ignore=entrypoints/serve/dev/rpc
  - PYTHONPATH=/vllm-workspace pytest -v -s entrypoints/serve/dev/rpc

- label: COPY-C Entrypoints Integration (API Server OpenAI - Part 1) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai --ignore=entrypoints/openai/completion --ignore=entrypoints/openai/chat_completion --ignore=entrypoints/openai/responses --ignore=entrypoints/openai/correctness

- label: COPY-C Entrypoints Integration (API Server OpenAI - Part 2) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai/chat_completion
  - pytest -v -s entrypoints/openai/completion --ignore=entrypoints/openai/completion/test_tensorizer_entrypoint.py

- label: COPY-C Entrypoints Integration (API Server Generate) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s tool_use
  - pytest -v -s entrypoints/tool_parsers
  - pytest -v -s entrypoints/generate
  - pytest -v -s entrypoints/anthropic

- label: COPY-C Entrypoints Integration (Responses API) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/openai/responses

- label: COPY-C Entrypoints Integration (Speech to Text) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/speech_to_text

- label: COPY-C Entrypoints Integration (Multimodal)
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/multimodal

- label: COPY-C Entrypoints Integration (Pooling) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  fast_check: true
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s entrypoints/pooling

- label: COPY-C OpenAI API correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash ../tools/install_torchcodec_rocm.sh || exit 1
  - pytest -s entrypoints/openai/correctness/

#-----------------------------------------------------------  mi300 · evals  -----------------------------------------------------------#

- label: COPY-C DeepSeek V2-Lite Prefetch Offload Accuracy (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  num_gpus: 1
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/deepseek_v2_lite_prefetch_offload.sh 0.25 200 8030

- label: COPY-C LM Eval Small Models # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -s -v evals/gsm8k/test_gsm8k_correctness.py --config-list-file=configs/models-small.txt

- label: COPY-C LM Eval Small Models (MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-small-rocm.txt

- label: COPY-C Multi-Modal Accuracy Eval (Small Models) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-mm-small.txt --tp-size=1

- label: COPY-C GPQA Eval (GPT-OSS) (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
    - uv pip install --system 'gpt-oss[eval]==0.0.5'
    - pytest -s -v evals/gpt_oss/test_gpqa_correctness.py --config-list-file=configs/models-gfx942.txt

- label: COPY-C DeepSeek V2-Lite Sync EPLB Accuracy (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/deepseek_v2_lite_ep_eplb.sh 0.25 200 8010

- label: COPY-C LM Eval Large Models (4xA100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large.txt --tp-size=4

- label: COPY-C Qwen3-30B-A3B-FP8-block Sync EPLB Accuracy (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/qwen30b_a3b_fp8_block_ep_eplb.sh 0.8 200 8020

- label: COPY-C Qwen3-Next-80B-A3B-Instruct MTP Async EPLB Accuracy # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace"
  commands:
  - bash .buildkite/scripts/scheduled_integration_test/qwen3_next_mtp_async_eplb.sh 0.8 1319 8040

- label: COPY-C LM Eval Large Models (8xH200-8xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v evals/gsm8k/test_gsm8k_correctness.py --config-list-file=configs/models-mi3xx.txt

#---------------------------------------------------------  mi300 · examples  ----------------------------------------------------------#

- label: COPY-C Examples # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/examples"
  commands:
    - pip install tensorizer
    # Basic
    - python3 basic/offline_inference/chat.py
    - python3 basic/offline_inference/generate.py --model facebook/opt-125m
    - python3 basic/offline_inference/generate.py --model meta-llama/Llama-2-13b-chat-hf --cpu-offload-gb 10
    - python3 basic/offline_inference/classify.py
    - python3 basic/offline_inference/embed.py
    - python3 basic/offline_inference/score.py
    # Multi-modal models
    - python3 generate/multimodal/audio_language_offline.py --seed 0
    - python3 generate/multimodal/vision_language_offline.py --seed 0
    - python3 generate/multimodal/vision_language_multi_image_offline.py --seed 0
    - python3 generate/multimodal/encoder_decoder_multimodal_offline.py --model-type whisper --seed 0
    # Pooling models
    - python3 pooling/embed/vision_embedding_offline.py --seed 0
    # Features demo
    - python3 features/automatic_prefix_caching/prefix_caching_offline.py
    - python3 deployment/llm_engine_example.py
    - python3 features/tensorize_vllm_model.py --model facebook/opt-125m serialize --serialized-directory /tmp/ --suffix v1 && python3 features/tensorize_vllm_model.py --model facebook/opt-125m deserialize --path-to-tensors /tmp/vllm/facebook/opt-125m/v1/model.tensors
    - python3 features/speculative_decoding/spec_decode_offline.py --test --method eagle --num_spec_tokens 3 --dataset-name hf --dataset-path philschmid/mt-bench --num-prompts 80 --temp 0 --top-p 1.0 --top-k -1 --tp 1 --enable-chunked-prefill --max-model-len 2048
    - python3 features/speculative_decoding/spec_decode_offline.py --test --method eagle3 --num_spec_tokens 3 --dataset-name hf --dataset-path philschmid/mt-bench --num-prompts 80 --temp 0 --top-p 1.0 --top-k -1 --tp 1 --enable-chunked-prefill --max-model-len 1536

#----------------------------------------------------------  mi300 · kernels  ----------------------------------------------------------#

- label: COPY-C Kernels Attention Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/attention --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: COPY-C Kernels Core Operation Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/core --ignore=kernels/core/test_minimax_reduce_rms.py  kernels/test_concat_mla_q.py kernels/test_top_k_per_row.py

- label: COPY-C Kernels MoE Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 5
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/moe --ignore=kernels/moe/test_modular_oai_triton_moe.py --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT
  - pytest -v -s kernels/moe/test_modular_oai_triton_moe.py --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: COPY-C Kernels Quantization Test %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s kernels/quantization --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT

- label: COPY-C Kernels FP8 MoE Test (2xH100-2xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s kernels/moe/test_deepep_moe.py

#-----------------------------------------------------------  mi300 · lora  ------------------------------------------------------------#

- label: COPY-C LoRA %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s lora --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --ignore=lora/test_chatglm3_tp.py --ignore=lora/test_llama_tp.py --ignore=lora/test_qwen3_with_multi_loras.py --ignore=lora/test_olmoe_tp.py --ignore=lora/test_deepseekv2_tp.py --ignore=lora/test_gptoss_tp.py --ignore=lora/test_qwen3moe_tp.py --ignore=lora/test_qwen35_densemodel_lora.py

- label: COPY-C LoRA TP (Distributed) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
  - pytest -v -s -x lora/test_chatglm3_tp.py
  - pytest -v -s -x lora/test_llama_tp.py
  - pytest -v -s -x lora/test_qwen3_with_multi_loras.py
  - pytest -v -s -x lora/test_olmoe_tp.py
  - pytest -v -s -x lora/test_gptoss_tp.py
  - pytest -v -s -x lora/test_qwen35_densemodel_lora.py

#------------------------------------------------------  mi300 · model_executor  -------------------------------------------------------#

- label: COPY-C Model Executor # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - apt-get update && apt-get install -y curl libsodium23
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s model_executor -m '(not slow_test)'
  - pytest -v -s entrypoints/openai/completion/test_tensorizer_entrypoint.py

#------------------------------------------------------  mi300 · models / basic  -------------------------------------------------------#

- label: COPY-C Basic Models Tests (Extra Initialization) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_initialization.py -k 'not test_can_initialize_small_subset' --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

- label: COPY-C Basic Models Tests (Initialization) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_initialization.py::test_can_initialize_small_subset

- label: COPY-C Basic Models Tests (Other) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/test_terratorch.py models/test_transformers.py models/test_registry.py

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: COPY-C Language Models Test (Extended Pooling)  # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/pooling -m 'not core_model'

- label: COPY-C Language Models Tests (Standard) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip freeze | grep -E 'torch'
  - pytest -v -s models/language -m 'core_model and (not slow_test)'

- label: COPY-C Language Models Tests (Extra Standard) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip freeze | grep -E 'torch'
  - pytest -v -s models/language -m 'core_model and slow_test' --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#----------------------------------------------------  mi300 · models / multimodal  ----------------------------------------------------#

- label: COPY-C Multi-Modal Models (Extended Generation 1) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation -m 'not core_model' --ignore models/multimodal/generation/test_common.py
  - pytest -v -s models/multimodal/test_mapping.py

- label: COPY-C Multi-Modal Models (Extended Generation 2) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m 'split(group=0) and not core_model'


- label: COPY-C Multi-Modal Models (Extended Generation 3) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m 'split(group=1) and not core_model'

- label: "COPY-C Multi-Modal Models (Standard) 1: qwen2" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "qwen2"
  - pytest -v -s models/multimodal/generation/test_ultravox.py -m core_model

- label: "COPY-C Multi-Modal Models (Standard) 3: llava + qwen2_vl" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "not qwen2 and not qwen3 and not gemma"
  - pytest -v -s models/multimodal/generation/test_qwen2_vl.py -m core_model

- label: "COPY-C Multi-Modal Models (Standard) 4: other + whisper" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal -m core_model --ignore models/multimodal/generation/test_common.py --ignore models/multimodal/generation/test_ultravox.py --ignore models/multimodal/generation/test_qwen2_5_vl.py --ignore models/multimodal/generation/test_qwen2_vl.py --ignore models/multimodal/generation/test_whisper.py  --ignore models/multimodal/generation/test_memory_leak.py --ignore models/multimodal/processing
  - pytest -v -s models/multimodal/generation/test_memory_leak.py -m core_model
  - cd .. && VLLM_WORKER_MULTIPROC_METHOD=spawn pytest -v -s tests/models/multimodal/generation/test_whisper.py -m core_model

- label: COPY-C Multi-Modal Processor # 1h 42m
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/processing/test_tensor_schema.py

- label: COPY-C Multi-Modal Processor (CPU) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/processing --ignore models/multimodal/processing/test_tensor_schema.py --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#-----------------------------------------------------  mi300 · models / quantized  -----------------------------------------------------#

- label: COPY-C Quantized Models Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/quantization

#--------------------------------------------------  mi300 · models / transformers  ---------------------------------------------------#

- label: COPY-C Transformers Nightly Models (Shardable) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  parallelism: 4
  working_dir: "/vllm-workspace/"
  commands:
  - pip install --upgrade git+https://github.com/huggingface/transformers
  - pytest -v -s tests/models/test_initialization.py --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB
  - pytest -v -s tests/models/multimodal/processing/ --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

- label: COPY-C Transformers Nightly Models (Single) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/"
  commands:
  - pip install --upgrade git+https://github.com/huggingface/transformers
  - pytest -v -s tests/models/test_transformers.py
  - pytest -v -s tests/models/multimodal/test_mapping.py
  - python3 examples/basic/offline_inference/chat.py
  - python3 examples/generate/multimodal/vision_language_offline.py --model-type qwen2_5_vl
  - VLLM_WORKER_MULTIPROC_METHOD=spawn python3 examples/generate/multimodal/audio_language_offline.py --model-type whisper

#----------------------------------------------------------  mi300 · plugins  ----------------------------------------------------------#

- label: COPY-C Plugin Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  # BEGIN: platform plugin and general plugin tests, all the code in-between runs on dummy platform
  - pip install -e ./plugins/vllm_add_dummy_platform
  - pytest -v -s plugins_tests/test_platform_plugins.py
  - pip uninstall vllm_add_dummy_platform -y
  # END: platform plugin tests
  # BEGIN: `io_processor` plugins test, all the code in between uses the `prithvi_io_processor` plugin
  - pip install -e ./plugins/prithvi_io_processor_plugin
  - pytest -v -s plugins_tests/test_io_processor_plugins.py
  - pytest -v -s plugins_tests/test_terratorch_io_processor_plugins.py
  - pip uninstall prithvi_io_processor_plugin -y
  # END: `io_processor` plugins test
  # BEGIN: `bge_m3_sparse io_processor` test
  - pip install -e ./plugins/bge_m3_sparse_plugin
  - pytest -v -s plugins_tests/test_bge_m3_sparse_io_processor_plugins.py
  - pip uninstall bge_m3_sparse_plugin -y
  # END: `bge_m3_sparse io_processor` test
  # BEGIN: `colbert_query io_processor` test
  - pip install -e ./plugins/colbert_query_plugin
  - pytest -v -s plugins_tests/test_colbert_query_io_processor_plugins.py
  - pip uninstall colbert_query_plugin -y
  # END: `colbert_query io_processor` test
  # BEGIN: `stat_logger` plugins test
  - pip install -e ./plugins/vllm_add_dummy_stat_logger
  - pytest -v -s plugins_tests/test_stats_logger_plugins.py
  - pip uninstall dummy_stat_logger -y
  # END: `stat_logger` plugins test
  # BEGIN: other tests
  - pytest -v -s plugins_tests/test_scheduler_plugins.py
  - pip install -e ./plugins/vllm_add_dummy_model
  - pytest -v -s distributed/test_distributed_oot.py
  - pytest -v -s plugins_tests/test_oot_registration_online.py # it needs a clean process
  - pytest -v -s plugins_tests/test_oot_registration_offline.py # it needs a clean process
  - pytest -v -s plugins_tests/lora_resolvers # unit tests for in-tree lora resolver plugins

#-------------------------------------------------------  mi300 · quantization  --------------------------------------------------------#

- label: COPY-C Quantization # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:

  # temporary install here since we need nightly, will move to requirements/test.in
  # after torchao 0.12 release, and pin a working version of torchao nightly here

  # since torchao nightly is only compatible with torch nightly currently
  # https://github.com/pytorch/ao/issues/2919, we'll have to skip new torchao tests for now
  # we can only upgrade after this is resolved
  # TODO(jerryzh168): resolve the above comment
  - uv pip install --system torchao==0.17.0
  - uv pip install --system conch-triton-kernels
  - VLLM_TEST_FORCE_LOAD_FORMAT=auto pytest -v -s quantization/ --ignore quantization/test_blackwell_moe.py

#-----------------------------------------------------------  mi300 · rocm  ------------------------------------------------------------#

- label: COPY-C ROCm AITER Ops Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s rocm/aiter/

#---------------------------------------------------------  mi300 · samplers  ----------------------------------------------------------#

- label: COPY-C Samplers Test # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s samplers

#------------------------------------------------------------  mi300 · misc  ------------------------------------------------------------#

- label: COPY-C Python-only Installation # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash standalone_tests/python_only_compile.sh

- label: COPY-C Regression # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install modelscope
  - pytest -v -s test_regression.py

#---------------------------------------------------------  mi300 · ray_compat  ---------------------------------------------------------#

- label: COPY-C Ray Dependency Compatibility Check # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/"
  commands:
  - bash /vllm-workspace/.buildkite/scripts/check-ray-compatibility.sh

#------------------------------------------------------------  mi300 · v1  -------------------------------------------------------------#

- label: COPY-C Acceptance Length Test (Large Models) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_ALLOW_INSECURE_SERIALIZATION=1
  - pytest -v -s v1/spec_decode/test_acceptance_length.py -m slow_test

- label: COPY-C e2e Scheduling (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/general/test_async_scheduling.py

- label: COPY-C Engine (1 GPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/engine/test_preprocess_error_handling.py
  - pytest -v -s v1/engine --ignore v1/engine/test_preprocess_error_handling.py

- label: COPY-C Spec Decode Draft Model # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "draft_model or no_sync or batch_inference"

- label: COPY-C Spec Decode Eagle # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "eagle_correctness"

- label: COPY-C Spec Decode Ngram + Suffix # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "ngram or suffix"

- label: COPY-C Spec Decode Speculators + MTP # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/e2e/spec_decode -k "speculators or mtp_correctness"

- label: COPY-C Speculators Correctness # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_ALLOW_INSECURE_SERIALIZATION=1
  - pytest -v -s v1/spec_decode/test_speculators_correctness.py -m slow_test

- label: COPY-C Extract Hidden States Integration # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -v -s v1/kv_connector/extract_hidden_states_integration

- label: COPY-C V1 attention (H100-MI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/attention

- label: COPY-C V1 Core + KV + Metrics # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - pytest -v -s -m 'not cpu_test' v1/core
  - pytest -v -s v1/executor
  - pytest -v -s v1/kv_offload
  - pytest -v -s v1/worker
  - pytest -v -s -m 'not cpu_test' v1/kv_connector/unit
  - pytest -v -s -m 'not cpu_test' v1/metrics
  - pip install -U git+https://github.com/robertgshaw2-redhat/lm-evaluation-harness.git@streaming-api
  # - export HSA_NO_SCRATCH_RECLAIM=1
  - pytest -v -s entrypoints/openai/correctness/test_lmeval.py::test_lm_eval_accuracy_v1_engine

- label: COPY-C V1 others (CPU) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  no_gpu: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s -m 'cpu_test' v1/core
  - pytest -v -s v1/structured_output
  - pytest -v -s v1/test_serial_utils.py
  - pytest -v -s -m 'cpu_test' v1/kv_connector/unit
  - pytest -v -s -m 'cpu_test' v1/metrics

- label: COPY-C V1 Sample + Logits # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s v1/sample
  - pytest -v -s v1/logits_processors
  - pytest -v -s v1/test_oracle.py
  - pytest -v -s v1/test_request.py
  - pytest -v -s v1/test_outputs.py

- label: COPY-C Distributed DP Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_async_llm_dp.py
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_eagle_dp.py
  - TP_SIZE=1 DP_SIZE=2 pytest -v -s v1/distributed/test_external_lb_dp.py
  - DP_SIZE=2 pytest -v -s entrypoints/openai/test_multi_api_servers.py

- label: COPY-C Metrics, Tracing (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - "pip install \
      'opentelemetry-sdk>=1.26.0' \
      'opentelemetry-api>=1.26.0' \
      'opentelemetry-exporter-otlp>=1.26.0' \
      'opentelemetry-semantic-conventions-ai>=0.4.1'"
  - pytest -v -s v1/tracing

- label: COPY-C V1 e2e (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s v1/e2e/spec_decode/test_spec_decode.py -k "tensor_parallelism"

- label: COPY-C NixlConnector PD + Spec Decode acceptance (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_spec_decode_test.sh

- label: COPY-C CrossLayer KV layout Distributed NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - CROSS_LAYERS_BLOCKS=True ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: COPY-C Distributed DP Tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_async_llm_dp.py
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_eagle_dp.py
  - TP_SIZE=2 DP_SIZE=2 pytest -v -s v1/distributed/test_external_lb_dp.py
  - TP_SIZE=1 DP_SIZE=4 pytest -v -s v1/distributed/test_internal_lb_dp.py
  - TP_SIZE=1 DP_SIZE=4 pytest -v -s v1/distributed/test_hybrid_lb_dp.py
  - pytest -v -s v1/engine/test_engine_core_client.py::test_kv_cache_events_dp
  - pytest -v -s distributed/test_utils.py

- label: COPY-C Distributed NixlConnector PD accuracy (4 GPUs)  # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: COPY-C DP EP Distributed NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - DP_EP=1 ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: COPY-C Hybrid SSM NixlConnector PD accuracy tests (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system -r /vllm-workspace/requirements/kv_connectors_rocm.txt
  - HYBRID_SSM=1 ATTENTION_BACKEND=TRITON_ATTN bash v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh

- label: COPY-C V1 e2e (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  working_dir: "/vllm-workspace/tests"
  commands:
    - pytest -v -s v1/e2e/spec_decode/test_spec_decode.py -k "eagle_correctness_heavy"

- label: COPY-C V1 e2e (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  commands:
    - pytest -v -s v1/e2e/test_hybrid_chunked_prefill.py

#------------------------------------------------------  mi300 · weight_loading  -------------------------------------------------------#

- label: COPY-C Weight Loading Multiple GPU # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash weight_loading/run_model_weight_loading_test.sh -c weight_loading/models-amd.txt

- label: COPY-C Weight Loading Multiple GPU - Large Models # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - bash weight_loading/run_model_weight_loading_test.sh -c weight_loading/models-large-amd.txt

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#----------------------------------------------------------  mi300 · compile  ----------------------------------------------------------#

- label: COPY-C Distributed Compile + RPC Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s entrypoints/llm/test_collective_rpc.py
  - pytest -v -s ./compile/fullgraph/test_basic_correctness.py
  - pytest -v -s ./compile/test_wrapper.py

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: COPY-C Distributed Torchrun + Shutdown Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - VLLM_TEST_SAME_HOST=1 torchrun --nproc-per-node=4 distributed/test_same_node.py | grep 'Same node test passed'
  - VLLM_TEST_SAME_HOST=1 VLLM_TEST_WITH_DEFAULT_DEVICE_SET=1 torchrun --nproc-per-node=4 distributed/test_same_node.py | grep 'Same node test passed'
  - CUDA_VISIBLE_DEVICES=0,1 pytest -v -s v1/shutdown
  - pytest -v -s v1/worker/test_worker_memory_snapshot.py

- label: COPY-C Distributed Compile + Comm (4 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s compile/fullgraph/test_basic_correctness.py
  - pytest -v -s distributed/test_pynccl.py
  - pytest -v -s distributed/test_events.py
  - pytest -v -s distributed/test_symm_mem_allreduce.py
  - pytest -v -s distributed/test_multiproc_executor.py::test_multiproc_executor_multi_node

#----------------------------------------------------------  mi300 · engine  -----------------------------------------------------------#

- label: COPY-C Engine # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s engine test_sequence.py test_config.py test_logger.py test_vllm_port.py test_jit_monitor.py

#-----------------------------------------------------------  mi300 · evals  -----------------------------------------------------------#

- label: COPY-C LM Eval Large Models (4xH100-4xMI300) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_4
  num_gpus: 4
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_USE_DEEP_GEMM=0
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large-rocm-fp8.txt --tp-size=4

- label: COPY-C ROCm LM Eval Large Models (8 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_8
  num_gpus: 8
  working_dir: "/vllm-workspace/.buildkite/lm-eval-harness"
  commands:
  - export VLLM_WORKER_MULTIPROC_METHOD=spawn
  - pytest -s -v test_lm_eval_correctness.py --config-list-file=configs/models-large-rocm.txt --tp-size=8

#-----------------------------------------------------  mi300 · models / language  -----------------------------------------------------#

- label: COPY-C Language Models Test (Extended Generation) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system --no-build-isolation 'git+https://github.com/AndreasKaratzas/mamba@fix-rocm-7.0-warp-size-constexpr'
  - uv pip install --system --no-build-isolation 'git+https://github.com/Dao-AILab/causal-conv1d@v1.6.0'
  - pytest -v -s models/language/generation -m '(not core_model) and (not hybrid_model)'

- label: COPY-C Language Models Tests (Hybrid) %N # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  parallelism: 2
  working_dir: "/vllm-workspace/tests"
  commands:
  - uv pip install --system --no-build-isolation 'git+https://github.com/AndreasKaratzas/mamba@fix-rocm-7.0-warp-size-constexpr'
  - uv pip install --system --no-build-isolation 'git+https://github.com/Dao-AILab/causal-conv1d@v1.6.0'
  - pytest -v -s models/language/generation -m hybrid_model --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB

#----------------------------------------------------  mi300 · models / multimodal  ----------------------------------------------------#

- label: COPY-C Multi-Modal Models (Extended Pooling) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/multimodal/pooling -m 'not core_model'

- label: "COPY-C Multi-Modal Models (Standard) 2: qwen3 + gemma" # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  torch_nightly: true
  working_dir: "/vllm-workspace/tests"
  commands:
  - pip install git+https://github.com/TIGER-AI-Lab/Mantis.git
  - pytest -v -s models/multimodal/generation/test_common.py -m core_model -k "qwen3 or gemma"
  - pytest -v -s models/multimodal/generation/test_qwen2_5_vl.py -m core_model

#------------------------------------------------------------  mi300 · v1  -------------------------------------------------------------#

- label: COPY-C V1 Spec Decode # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s -m 'not slow_test' v1/spec_decode

#########################################################################################################################################
#                                                                                                                                       #
#                                                         MI300 (gfx942) tests                                                          #
#                                                                                                                                       #
#########################################################################################################################################

#--------------------------------------------------------  mi300 · distributed  --------------------------------------------------------#

- label: COPY-C Distributed Quick Reduce Tests (2 GPUs) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_2
  num_gpus: 2
  working_dir: "/vllm-workspace/"
  commands:
  - pytest -v -s tests/distributed/test_rocm_quick_reduce.py
  - pytest -v -s tests/distributed/test_quick_all_reduce.py

- label: COPY-C Language Models Test (PPL) # TBD
  timeout_in_minutes: 180
  mirror_hardwares: [amdexperimental, amdshadow]
  agent_pool: mi300_1
  working_dir: "/vllm-workspace/tests"
  commands:
  - pytest -v -s models/language/generation_ppl_test
