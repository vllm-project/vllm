group: Attention XPU
depends_on:
  - image-build-xpu
steps:
- label: V1 attention (H100-MI300)
  device: intel_gpu
  timeout_in_minutes: 85
  parallelism: 2
  working_dir: .
  no_plugin: true
  env:
    REGISTRY: public.ecr.aws/q9t5s3a7
    REPO: vllm-ci-test-repo
    VLLM_TEST_DEVICE: xpu
  agent_tags:
    label: debug
    gpu: 1+
  _raw_old_commands:
  - pytest -v -s v1/attention --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT
  source_file_dependencies:
  - vllm/config/attention.py
  - vllm/model_executor/layers/attention
  - vllm/v1/attention
  - tests/v1/attention
  commands:
  - >-
    bash .buildkite/scripts/hardware_ci/run-intel-test.sh
    'cd tests &&
    pytest -v -s v1/attention --shard-id=$$BUILDKITE_PARALLEL_JOB --num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT || true'
